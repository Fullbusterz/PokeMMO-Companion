@AGENTS.md

# PokeMMO Companion App — Contexto del Proyecto

## Qué es esto

App móvil companion para PokeMMO (no para Pokémon oficial — son cosas distintas, ver regla de oro más abajo). Proyecto personal de Ferran, en su tiempo libre, sin presión comercial. Construida con Claude Code, supervisada por Ferran.

**Por qué existe:** no hay ninguna app pulida y nativa en Play Store/App Store para PokeMMO. Lo que existe son webs (PokeMMO Hub) y herramientas sueltas de aficionados en GitHub/itch.io, ninguna en tienda oficial, ninguna bilingüe ES/EN.

**Objetivo explícito de este proyecto:** NO es un proyecto para ganar dinero. Se construye independientemente de si genera ingresos o no. Esto tiene implicaciones prácticas — ver sección "Qué NO hacer en v1".

---

## 📍 ESTADO ACTUAL (2026-07-12) — leer esto primero

Repo en GitHub: [github.com/Fullbusterz/PokeMMO-Companion](https://github.com/Fullbusterz/PokeMMO-Companion), remoto `origin`, rama `master`.

**🎉 Las 5 fases del roadmap original están completas** (Torneos/ligas, Pokédex+Guía+tiers, cría, Showdown export+tracker de shinies, calculadora de daño), **más la tanda de mejoras/features del 2026-07-12** (ver bloque siguiente). ⚠️ Sigue habiendo MUCHO trabajo sin comitear (todo desde el commit `6d20ee5`) — Ferran aún no ha dado el OK al commit; preguntárselo al empezar la próxima sesión.

### 🔜 PRÓXIMA SESIÓN — seguimos donde lo dejamos, empezar aquí directamente

**[SESIÓN 2026-07-15, tanda autónoma — Ferran delegó con "haz todo lo que puedas"] PWA desplegada + feature de MTs + confirmaciones web.** Commits `7d470fb` (config EAS), `1d1156c` (PWA) y `144090a` (resto), todos pusheados.
1. **🌐 PWA VIVA en https://fullbusterz.github.io/PokeMMO-Companion/** — export web `output: "single"` (SPA; "static" exigiría generateStaticParams para 649 fichas dinámicas), `experiments.baseUrl: "/PokeMMO-Companion"`, manifest + service worker (cache versionada, cache-first para /_expo/ hasheados, network-first para navegaciones) + iconos PWA 192/512/maskable en `public/` (el head se personaliza vía `public/index.html` de `expo customize` — con output "single" NO existe `+html.tsx`), 404.html=index.html para deep links. **Despliegue AUTOMÁTICO: cada push a master corre `.github/workflows/deploy-web.yml`** (npm ci --legacy-peer-deps + expo export + actions/deploy-pages; Pages ya en build_type=workflow vía gh api). ⚠️ Tres orígenes = tres storages separados (PWA/localhost/APK futura) — el puente es la Copia de seguridad por código.
2. **"¿Quién aprende este movimiento?"** — `data/reference/move-learners.json` (índice inverso 556 movimientos → ids, ~464KB, import normal) + `src/lib/moveLearners.ts`: `getLearners()` y `getRecommendedLearners()` (solo movimientos de daño — para estado devuelve `null` a propósito; razones ESTRUCTURADAS hasStab/attackStat/tier, orden STAB→stat crudo, el tier se muestra pero JAMÁS ordena — misma doctrina que suggestCandidates; **los tier `null` (no obtenibles: Groudon, Mewtwo...) se EXCLUYEN de recomendados** — se detectó en verificación en vivo que Groudon salía nº1 para Terremoto — pero siguen en la lista completa). UI en el MoveDetailModal de la ficha (top-5 con chips de razón + lista completa expandible navegable) e intención nueva del Oráculo ES/EN ("quién aprende X"/"who learns X"). Verificado en vivo: Terremoto → Rhyperior/Excadrill/Rhydon/Garchomp/Mamoswine, 203 aprendices.
3. **Confirmaciones de borrado migradas a `confirmDestructive`** (torneos en `app/torneos/index.tsx` — ojo, el borrado vive en la LISTA, no en [id] —, shinies, sesiones EV; caja ya lo usaba). Los Alert informativos de validación se dejaron como Alert a propósito.
4. **Sueltos**: `pokemmo_tiers.json` adelgazado 252→130KB (source deduplicado, metodología completa aquí); nombres de Pokémon por locale en caja/BoxPickerModal — **pero verificado: los 649 nombres ES son idénticos a los EN** (español oficial usa los nombres ingleses), así que el `name.es` fijo del resto de la app es INOFENSIVO — no "arreglarlo" sin motivo.
5. **[BUG MÓVIL ARREGLADO 2026-07-15] Tap en objeto oculto plegaba la tarjeta de la Guía (reportado por Ferran probando la PWA en su Android).** Causa raíz: en react-native-web TÁCTIL, un `<Text onPress>` desnudo NO negocia el responder de gestos como lo hace un Pressable — el `PressScale` ancestro (que envolvía la tarjeta ENTERA con el toggle de plegado) se quedaba el touch antes de que el `stopPropagation` del texto pudiera actuar (con ratón sí funcionaba, por eso nunca se vio en el preview de escritorio). **Arreglo estructural, no otro parche de propagación**: en `LocationCard` (app/guia/[region]/index.tsx) solo la CABECERA es ahora el PressScale de plegado; el cuerpo expandido es un HERMANO fuera de todo Pressable de colapso. Lección general para esta app: nunca envolver contenido interactivo (Text onPress, chips) dentro de un Pressable contenedor y confiar en stopPropagation — en táctil no basta; separar las zonas pulsables estructuralmente. Verificado en vivo: cuerpo no pliega, palabra resaltada abre la imagen, cabecera alterna, marcapáginas intacto.
6. **Rendimiento nativo — PENDIENTE DE FERRAN**: reportó que "la app va fatal" en el móvil pero no ha aclarado si probó el APK de EAS o Expo Go (modo desarrollo, lento por diseño). NO diagnosticar a ciegas: preguntarle primero qué instaló y qué síntomas exactos (arranque/scroll/pantallas concretas). Peso eager de arranque auditado: ~870KB de JSON (razonable), Pokédex ya virtualizada con FlatList.

**[SESIÓN 2026-07-12] Tanda grande: 4 mejoras + 3 features nuevas, orquestada con subagentes sonnet trabajando directo en el árbol (regla de delegación de Ferran mientras Fable es el modelo principal).** Todo verificado con tsc + tests node por pieza; verificación visual en Chrome real (el panel de preview integrado estuvo caído toda la sesión) quedó A MEDIAS — ver pendientes abajo.

1. **Backup global por código** (`src/lib/backup.ts` codec puro + `src/lib/backupStorage.ts` capa AsyncStorage + `app/backup/index.tsx` + tarjeta en home). Registro extensible `BACKUP_STORE_KEYS` — al añadir un store persistido nuevo hay que registrarlo ahí y en `REHYDRATE_BY_KEY` (los 3 features de esta sesión ya lo hacen; el backup cubre 6 stores: torneos, shinies, marcapáginas, capturas, sesiones EV, caja). Import valida todo (incl. integridad referencial de torneos vía parseImportedTournament) y rehidrata los stores zustand en caliente. **NOVEDAD IMPORTANTE — confirmaciones destructivas en web por fin funcionan**: `src/lib/confirmDialog.ts` (`confirmDestructive()`) envuelve Alert.alert (nativo) y `window.confirm` (web, que SÍ funciona a diferencia de Alert). ⚠️ Para testing automatizado del navegador: `window.confirm` es BLOQUEANTE — congela el renderer del tab hasta que un humano pulsa, no se puede automatizar por CDP. Considerar migrar los borrados de torneos/shinies/EVs a `confirmDestructive` también (hoy siguen con Alert.alert = no-op en web).
2. **Efectos de movimiento completados 556/556**: Ferran completó a mano (vía ChatGPT) casi todo `moves.json` y las 170 `ability-descriptions.json`; los 10 efectos que la propia wiki no tiene (Sand-Attack, Selfdestruct, SmellingSalt, SmokeScreen, Softboiled, SolarBeam, SonicBoom, ThunderPunch, ThunderShock, ViceGrip) se rellenaron desde Bulbapedia (efecto Gen 5, `verified:false`, fuente explícita). Dato confirmado de paso: **Softboiled 5 PP en PokeMMO no es errata** — PokeMMO nerfeó los PP de los movimientos de recuperación (oficial: 10).
3. **Tier list actualizada a 2026-04-29** (`data/tiers/pokemmo_tiers.json` rederivado): las páginas wiki/OU (622), wiki/UU (578) y wiki/NU (527) tienen los rosters completos por tier, editadas 2026-04-29 — extraídas con navegador real (el anti-bot Anubis de la wiki bloquea curl/API pero un navegador lo resuelve solo; las tablas se leyeron del DOM, ids de dex exactos, no resumen de IA). Derivación: NU⊂UU⊂OU verificado estricto, tier = diferencia de conjuntos, 27 no-obtenibles (complemento exacto de la tabla OU) quedan `null`, bans universales de wiki/PvP (Shaymin, Jirachi) → `uber`. Solo 5 cambios vs 2022: Dugtrio/Wobbuffet/Rayquaza dejan Uber (sus bans ahora son condicionales por habilidad/movimiento, no por especie), Jirachi/Shaymin entran. Clave i18n renombrada `tierUnverifiedNotice`→`tierSourceNotice` ("según la PokeMMO Wiki a fecha {{date}}"). **Los bans condicionales actuales (Hydreigon+Draco Meteor, Garchomp+Danza Espada, Dugtrio+Arena Trap, Gallade+Sharpness, Reinforced Boots) NO están modelados** — el dataset es por especie; si algún día importa, es una capa nueva.
4. **Enums de encuentros traducidos** (`localizedEncounterValue()` en pokedex.ts, claves `pokedex.encounter.*`): Hierba alta/Muy común/Mañana etc. en ficha y Oráculo, fallback al crudo si valor desconocido. El índice de objetos del Oráculo ya era por-locale (nota vieja de este archivo estaba desactualizada); se arregló un bug real de matching: consultas "pegadas" tipo "tinymushroom" no encontraban "Tiny Mushroom" (ahora hay `squishedName`).
5. **Doble eliminación generalizada a CUALQUIER n entre 4 y 32** (`doubleElimBracket.ts`): los byes solo existen en ronda 1 de ganadores; sus "huecos" en el bracket de perdedores se auto-resuelven con `autoAdvanceLosersByes`/`autoUndoLosersByes` (equivalente dinámico del isBye del bracket simple — partidos con ambos alimentadores bye no se generan, con uno solo se auto-avanzan al llegar su único jugador real). Gran final única intacta. Verificado: 145 torneos simulados (5×29 tamaños) con 7+1 invariantes sin fallos, round-trip de deshacer paso a paso en 9 tamaños, export→import de torneo a medias.
6. **Registro de capturas** (`src/store/caughtStore.ts`, clave `pokemmo-caught`): toggle en ficha (badge dorado, mismo lenguaje que bookmark de guía), filtro Todos/Capturados/Me faltan en la lista, progreso X/N por región o X/649 global. Verificado en vivo en Chrome.
7. **Contador de EVs** (`src/lib/evTraining.ts` puro + `src/store/evSessionStore.ts` + `app/entreno/`): sesiones con objetivo por stat, botones +KO/+horda con EVs reales de `ev-training.json`, cap duro en 252/stat, aviso >510 sin bloquear, deshacer LIFO global por sesión. Tarjeta en home (icono fitness).
8. **Mi caja** (`src/store/boxStore.ts` clave `pokemmo-box` + `app/caja/` + `src/components/BoxPickerModal.tsx`): builds (especie/naturaleza/habilidad/objeto/IVs/EVs/≤4 movimientos del moveset real). `BoxBuild` EXTIENDE `TeamMemberBuild` de showdown.ts a propósito (export Showdown gratis). Integraciones: export Showdown por build, precarga en calculadora de daño (ambos lados, restringe chips de movimiento a los del build), y creador de equipos con override de "movimientos reales" en `analyzeOffensiveGaps(team, realMovesByPokemonId?)` (badge "Movimientos reales" vs "Movepool completo"; el override se deriva en vivo del store, no copia congelada). `groupMovesByMethod()` extraído de la ficha a pokedex.ts (compartido con el editor).

**⚠️ PENDIENTES INMEDIATOS de esta sesión:**
- **Verificación visual COMPLETA en Chrome real (2026-07-12)**: home (10 tarjetas), ficha Pokédex (tier "según la wiki a fecha 2026-04-29", toggle capturas funcionando, encuentros en ES: Hierba alta/Muy raro/Horda), lista (1/649 + filtro Capturados), backup punta a punta (export → import corrupto rechazado con mensaje → round-trip restaura y rehidrata en caliente), Mi caja (editor de Garchomp: habilidades/naturalezas/EVs 0-510/movimientos del moveset real en ES, contador 1/4), Entreno de EVs (crear sesión, selector de zonas reales por stat, +50 horda/+10 KO, deshacer exacto), y doble eliminación con n=6 (byes auto-avanzados a R2 de ganadores, perdedor real cae al bracket de perdedores, gran final única). Solo el Oráculo bilingüe quedó sin re-probar visualmente (verificado por test node del agente). Para automatizar el navegador con `window.confirm` presente: stubearlo con `window.confirm = () => true` vía JS antes del click (el diálogo real congela el tab para CDP). Los datos de prueba (torneo "Prueba doble 6", sesión "Garchomp físico", build de Garchomp, Pikachu capturado) quedaron en el localStorage de Chrome de Ferran en localhost:8081 — son inofensivos, borrar si molestan.
- **[COMPLETO 2026-07-12/15] Logo definitivo integrado** — la mochila roja de Ferran (fuente: `C:\Users\Ferra\Downloads\PokeMMOCompanionLogo.png`, 1254×1254) sustituye a la "chincheta viajera" en los 6 assets (icon/splash 1024, favicon 48, adaptive background 512 plano `#010207`, foreground 512 con la mochila extraída por flood-fill desde bordes y centrada en la zona segura ~66%, monochrome 432 silueta blanca — el hueco del asa queda macizo porque es una isla cerrada inalcanzable por el flood-fill, aceptado). `app.json` `android.adaptiveIcon.backgroundColor` actualizado a `#010207`. Pipeline: **sharp instalado en un dir temporal FUERA del repo** (`%TEMP%\claude\iconwork`) — mucho más simple que el pipeline Canvas+servidor de la chincheta; reutilizable ahí mismo si hay que regenerar. Verificado por dimensiones IHDR, canales alfa (esquinas transparentes, centro opaco) y visualmente (composición foreground+background idéntica al logo). Sigue pendiente el rebuild nativo para verlo en un dispositivo real (limitación de siempre).
- **Commit pendiente**: TODO el trabajo desde `6d20ee5` (2026-07-04) sigue sin comitear. Propuesto a Ferran, sin respuesta aún.
- Ideas que Ferran descartó explícitamente en esta tanda (no proponer de nuevo sin motivo): checklist de objetos en la Guía, estadísticas históricas del grupo de torneos.

**[COMPLETO 2026-07-11] Insignia de repuesto rediseñada — estilo "silueta no revelada".** Ferran no le gustaba cómo quedaba el degradado plano + icono de pata genérico para las ~464 especies sin sprite real. Pidió intentar primero ampliar la cobertura con una segunda fuente de arte fan-made, y si no había éxito, pasar a rediseñar el propio fallback.
- **Segunda fuente de arte — investigada y descartada**: se buscaron más candidatos en DeviantArt/PokéCommunity. "Fistipuffs — Pokemon Sprite Dex" resultó ser fakemon (criaturas inventadas, no Pokémon reales). "kittehkhat — All Pokemon Sprites (Gen 1-4)" y "Tobis-a-goodboy — Mega Sprite Dex" no tenían ninguna declaración de licencia, y al descargar y mirar la imagen real (no solo la descripción) resultaron ser compilaciones lisas y llanas de los sprites oficiales de Nintendo — exactamente lo que se está evitando. Ninguno sirve; no se sustituyó nada con estas fuentes.
- **`src/components/PokemonSprite.tsx` rediseñado**: el fallback ya no es un `LinearGradient` con icono de pata — ahora es una tarjeta oscura (`ink.800`) con un borde en el color del tipo principal y un "?" centrado, el mismo lenguaje visual que usan los juegos oficiales para un Pokémon "no visto todavía". Sigue sin dibujar ningún parecido real del Pokémon — solo tipo (dato de juego) + un símbolo genérico.
- Verificado en el preview: Weedle (#13, sin arte) muestra la tarjeta con borde verde Bicho y "?", tanto en la lista de la Pokédex como en la ficha grande (144px); Bulbasaur (#1, con arte real) no se vio afectado.

**[COMPLETO 2026-07-11] Aviso de tier competitivo simplificado.** Ferran pidió mostrar directamente qué tier es (Uber/OU/UU/NU) en vez del párrafo largo explicando la metodología. `pokedex.tierTitle` pasó de "Tier competitivo (PvP)" a "Tier competitivo"; `pokedex.tierUnverifiedNotice` pasó del párrafo completo a `"{{tier}} — no verificado desde {{date}}, puede haber cambiado."` (ES) / equivalente en EN — el nombre del tier se resuelve con el mismo `TIER_LABEL_KEYS` que ya usaba `TierBadge`. Verificado en el preview: Bulbasaur muestra "NU — no verificado desde 2022-03-24, puede haber cambiado."

**[COMPLETO 2026-07-11] Sprites oficiales de Nintendo (PokeAPI) sustituidos — cierra el hallazgo de la auditoría pre-Play Store.** Los 649 sprites venían de `raw.githubusercontent.com/PokeAPI/sprites` (arte oficial de Game Freak/Nintendo extraído del juego), lo que contradecía la regla ya escrita en este mismo archivo ("Sin usar sprites/assets oficiales de Nintendo si se puede evitar"). Ferran decidió sustituirlos por el trabajo de un artista de DeviantArt (KingOfThe-X-Roads, "ALL Pokemon sprites by me") que ya declara públicamente sus sprites "free for non-profit use" — pidió proceder ya y mandar igualmente un mensaje de confirmación directa (redactado y publicado como artifact) sin bloquear el trabajo a la espera de respuesta.
- **Cobertura real: 185 de 649 especies (~28%)** — el propio artista aún no ha dibujado el resto (su galería está marcada "[WIP]"). Nada de esto se rellenó con sprites oficiales como fallback: las ~464 especies sin arte disponible usan la insignia abstracta de tipo (ver punto siguiente), nunca Nintendo.
- **Localización y descarga**: la hoja de cálculo de Google enlazada desde la propia página de DeviantArt (`sprites by KingOfThe-X-Roads (individual)`) lista cada especie con un enlace `sta.sh` por generación (pestañas Gen 1-5, gids obtenidos navegando el iframe real ya que `pubhtml` no expone los gids en el HTML plano). Cada `sta.sh` se resuelve a una URL de imagen directa (con token firmado) vía la API pública `backend.deviantart.com/oembed` — permite bajar las 185 imágenes con `curl`/Node sin necesitar 185 navegaciones de navegador. Las descargas se guardaron en `assets/pokemon-sprites/{id}.png` (185 archivos, 96×96, ~600KB en total).
- **Emparejamiento nombre→id** (`src/lib/localPokemonSprites.ts`, generado, no editar a mano): normaliza nombres quitando sufijos entre paréntesis (formas, letras de Unown, placas de Arceus) y excluye explícitamente filas de formas regionales (Galar/Alola/Hisui/Paldea) que no existen en PokeMMO — evita que, p. ej., "Ponyta (Galar)" contamine la entrada real de Ponyta de Kanto. Caso especial: Nidoran♀/Nidoran♂ son entradas de Pokédex genuinamente distintas (no una variante cosmética como "Venusaur (Male)"/"(Female)", que sí colapsan a la misma especie) — resuelto con una clave de género aparte para no mezclarlas.
- **`src/components/PokemonSprite.tsx`** (nuevo, sustituye los 10 usos directos de `<Image source={{uri: pokemon.sprite}}>` repartidos por `app/index.tsx`, `app/pokedex/index.tsx`, `app/pokedex/[id].tsx` (ficha+fila de evolución), `app/equipos/index.tsx` (×3), `app/danio/index.tsx` (×2) y `app/shinies/index.tsx` (×2)): si el id tiene sprite local lo muestra: si no, cae a una insignia abstracta con gradiente de color de tipo(s) + icono genérico de pata (Ionicons `paw`) — nunca dibuja parecido de ningún Pokémon concreto, así que ni siquiera para las 464 especies sin arte del artista se corre riesgo de IP.
- **Limpieza de datos**: se eliminó el campo `sprite` (URL de PokeAPI) de los 649 registros en `data/*/pokemon.json` y del tipo `PokemonEntry` — ya no se usa en ningún sitio, dejarlo habría sido una referencia residual y confusa a un asset oficial que ya no se muestra. También se corrigió el texto de `source` en los 649 registros (mencionaba "sprite: PokeAPI" como procedencia, ya no es cierto).
- **Verificado en el preview vía inspección del DOM** (no capturas de pantalla — la herramienta de `screenshot` está fallando de forma intermitente en esta sesión, no relacionado con el código): Bulbasaur/Ivysaur/Venusaur (#1-3, con arte real) cargan el PNG local (`naturalWidth`/`naturalHeight` 96×96, sin error de carga) tanto en la lista de la Pokédex como en la ficha de detalle (versión grande, 144px); Weedle (#13, sin arte disponible) no renderiza ningún `<img>`, solo el `<div>` con gradiente Bicho/Veneno y el icono de pata — confirma que el fallback nunca intenta cargar nada de fuera.
- **Pendiente**: Ferran va a mandar el mensaje de confirmación directa al artista (ya redactado). Si responde que no, hay que quitar `assets/pokemon-sprites/` y `src/lib/localPokemonSprites.ts` y dejar la insignia abstracta como versión definitiva para las 649 especies (cambio de una sola línea en cada uno de los 10 call sites, ya que `PokemonSprite` ya maneja ambos casos por diseño).

**[COMPLETO 2026-07-11] Marcapáginas en las guías de región — "por dónde me quedé".** Pedido por Ferran, diseño dejado a mi criterio. Un marcador por región (no una lista — un jugador avanza por una región a la vez), persistido con el mismo patrón `zustand+persist`/AsyncStorage que el resto de stores del proyecto.
- **`src/store/guideBookmarkStore.ts`** (nuevo): `bookmarks: Partial<Record<RegionId, number>>` (región → `order` del tramo), `setBookmark`/`clearBookmark`. Guardar un tramo nuevo en una región simplemente sobrescribe el anterior — solo uno activo a la vez, por diseño.
- **`app/guia/[region]/index.tsx`**: icono de marcapáginas en cada `LocationCard` (`bookmark-outline` sin marcar, `bookmark` dorado relleno si es el marcador activo — con `stopPropagation` para no expandir/colapsar la tarjeta a la vez) + borde dorado más marcado en la tarjeta marcada para que destaque en la lista. Encima de la lista aparece un botón "Ir al marcador: {ubicación}" (solo si hay marcador en esa región) que limpia la búsqueda activa, expande el tramo marcado y hace scroll hasta él con `FlatList.scrollToIndex` (con el `onScrollToIndexFailed` recomendado por RN como red de seguridad, ya que las filas tienen alturas variables — colapsada vs. la única expandida — y `scrollToIndex` puede fallar si aún no las ha medido todas).
- **`app/guia/index.tsx`** (hub de regiones): cada tarjeta de región con marcador activo muestra "Marcador: {ubicación}" en vez del subtítulo genérico "Objetos y ruta", para saber por dónde ibas sin ni siquiera entrar a la región.
- Claves i18n nuevas (`guide.bookmarkAdd/bookmarkRemove/jumpToBookmark/bookmarkedAt`, ES y EN) — el nombre de ubicación mostrado usa `localizedLocationName()` ya existente, mismo criterio que el resto de la guía (display-only, nunca toca el `order`/`location` real usado para matching).
- Verificado en el preview de punta a punta: marcar Ciudad Verde (tramo 64 de Kanto) reemplaza el marcador anterior (Monte Moon), el botón "Ir al marcador" hace scroll desde el tramo 4 hasta el 64 y lo expande automáticamente, y el hub de Guía muestra "Marcador: Ciudad Verde" (ES) / "Bookmark: VIRIDIAN CITY" (EN, nombre sin traducir como corresponde) sin abrir la región.

**[COMPLETO 2026-07-11] Icono real de la app — "Chincheta viajera".** Sustituye el placeholder genérico de Expo, pendiente desde hacía varias sesiones. Se presentaron 4 conceptos en un artifact aparte (sello de mapa con Poké Ball recoloreada, chincheta viajera aislada, monograma serif, compás de explorador), todos derivados de la paleta ya aprobada de "Mapa de región" — Ferran eligió "Chincheta viajera": la chincheta roja del sendero de la home, aislada como icono, sin el marco de diamante ni el resto de la escena.
- **Problema técnico real:** no hay ninguna herramienta de rasterizado SVG→PNG disponible en este entorno (sin ImageMagick real — `convert` en Windows es el conversor de sistema de archivos FAT→NTFS, no ImageMagick —, sin `sharp` instalado). Resuelto dibujando el icono directamente con Canvas 2D (no SVG) en una página HTML servida por un mini servidor Node local (`http.createServer` en el puerto 5578, con un endpoint `POST /save/:name` que recibe el `Blob` del canvas vía `fetch` y lo escribe a `assets/` con `fs.writeFileSync`) — la página se abrió en el propio navegador de Claude Code, y `canvas.toBlob()` + `fetch()` subió cada PNG generado directamente al disco sin pasar el base64 por el contexto de la conversación (un primer intento pasando el base64 como texto de vuelta reventó tanto el límite de tokens de la herramienta como el límite de longitud de línea de comandos de Windows al intentar decodificarlo por `execSync`).
- **6 archivos generados y verificados con las dimensiones exactas que ya usaba el placeholder** (comprobadas leyendo el header IHDR del PNG a mano, sin librería): `icon.png` (1024×1024, marca completa con máscara de esquinas redondeadas), `android-icon-foreground.png` (512×512, transparente, solo la chincheta centrada dentro de la "zona segura" ~66% que exige el sistema de iconos adaptativos de Android), `android-icon-background.png` (512×512, relleno plano navy), `android-icon-monochrome.png` (432×432, transparente, silueta blanca pura con un agujero para el anillo — Android la tintará con el color de acento del sistema), `favicon.png` (48×48, versión simplificada sin el sendero punteado, que a ese tamaño se volvía ruido), `splash-icon.png` (1024×1024, misma marca completa que `icon.png`).
- **`app.json`**: de paso se corrigió `android.adaptiveIcon.backgroundColor`, que seguía en `#0B0E14` (color del tema oscuro original, previo al repaletado "Mapa de región" de esta misma sesión) — actualizado a `#0B1A1F`, el navy real ya usado en toda la app.
- Verificado visualmente abriendo cada PNG en el navegador (no solo comprobación de bytes): `icon.png` muestra la chincheta roja con anillo pergamino/dorado sobre fondo navy con esquinas redondeadas y el sendero punteado de fondo; `android-icon-monochrome.png` se lee con claridad como silueta de chincheta con el agujero del anillo, tal como necesita el sistema de temas de Android.
- **Pendiente para que se vea en un dispositivo real**: estos assets no son hot-reloadables — hace falta un rebuild nativo (`expo prebuild`/build de EAS) o al menos reiniciar `expo start` para que el icono nuevo se recoja; no verificado en simulador/dispositivo real en este entorno (mismo límite ya documentado del resto del proyecto).

**[COMPLETO 2026-07-11] Guía "PokeMMO vs. oficial: mecánicas" — cierra la Idea 1 de diferencias.** Segunda mitad pendiente desde la sesión anterior (la primera mitad, objeto por objeto, ya estaba en `differences.json`). Fuentes: PokeMMO Wiki (`wiki/Shiny_Pokémon` — bloqueada por el anti-bot, no se pudo usar — y `wiki/Alpha_Pokémon`, esta sí accesible) y PokeMMO Support oficial (`support.pokemmo.com/knowledgebase/article/shiny-rate`), ambas más autorevisadas que un hilo de foro.
- **Nuevo `data/guides/general-mechanics.json`** (tipo `ProseGuide`, mismo patrón que `ivs.json`/`effort-values.json` — reutiliza el renderer genérico `ProseSections`, sin componente nuevo): secciones sobre tasa de shiny (1/30.000 base, ~1/27.000 con Donador o Amuleto Shiny, ~1/24.000 combinando ambos — los castigos NO afectan a la tasa) y Pokémon Alfa (qué son, cómo aparecen los Alpha Swarms, mecánica de Wild Might y el truco de Contrario+Cambio de Habilidad, y la regla de cría — ambos progenitores deben ser Alfa o la cría nace normal).
- **Cría deliberadamente NO repetida** (ya tiene su propia guía dedicada, se linka en la nota) y **"movepools Gen 8" deliberadamente excluido**: no se encontró ninguna fuente específica de PokeMMO que confirme o describa esa diferencia (se revisó la wiki y varios hilos del foro) — se prefirió dejarlo fuera antes que inventar el dato, siguiendo la regla de oro del proyecto.
- Conectado en `guides.ts` (`ReferenceTopic`, `REFERENCE_TOPICS`, `getReferenceGuide`) y `app/guia/referencia/[topic].tsx` (añadido a la lista de topics que usan `ProseSections`) + claves i18n `guide.topics.general-mechanics` en ambos idiomas. Verificado en el preview: aparece en el índice de guías de referencia y la pantalla de detalle renderiza las 4 secciones sin errores.

**[COMPLETO 2026-07-11] Recuperadas 14 de las 69 capturas de ubicación bloqueadas por anti-bot.** Ferran pidió reintentar el hueco documentado. `curl` con un User-Agent de navegador normal sí pudo acceder a bastantes páginas de la wiki que antes habían dado bloqueo (el bloqueo original de ChatGPT no era permanente, solo intermitente/por volumen de peticiones).
- **Bug evitado durante el propio reintento**: la primera pasada del script "recuperó" 38 imágenes, pero 24 de ellas resolvían al mapa genérico de la región entera (`Map_Kanto.png`, `Map_Johto.png`, etc. — la misma imagen reutilizada en decenas de páginas de ruta) en vez de una captura específica de esa ubicación. Se filtraron a propósito y se descartaron, para no reintroducir el mismo bug de "imagen de ubicación equivocada" ya corregido antes en esta sesión (ver más abajo, `getLocationImage`). Solo se aceptaron las 14 imágenes con nombre de archivo específico de esa ubicación (`Lavender_Town.png`, `Hoenn_Route_101.png`, `Map_Johto_Route_39.png`...).
- **31 siguen bloqueadas de verdad**: el bloqueo resultó ser un challenge tipo Cloudflare ("Making sure you're not a bot!"), no una simple detección de User-Agent — reaparece tras varias peticiones seguidas incluso con pausas, y `curl` no puede resolver un challenge JS. Se decidió no seguir insistiendo (rendimientos decrecientes, y machacar la wiki con reintentos no es buena práctica).
- **`data/guides/location-images.json`**: ahora 139 ubicaciones con imagen verificada (antes 125) + 55 en `unverified` (antes 69, con el motivo actualizado por caso: "solo mapa genérico disponible" vs. "sigue bloqueado"). Verificado en el preview: Lavender Town (Kanto) y Dewford Town (Hoenn) cargan su captura específica correctamente (`naturalWidth` > 0, sin bloqueo de hotlink).

**[COMPLETO 2026-07-10] Repaletado visual completo — Dirección F "Mapa de región".** Ferran pidió más personalidad gráfica (la app se veía "genérica" — tarjetas planas idénticas con solo el icono cambiando). Se propusieron 6 mockups de dirección visual en un artifact aparte (arcade, carta holográfica, HUD esports, diario de entrenador, terminal del PC, mapa de región) y Ferran eligió la F.
- **Paleta (`src/theme/colors.js`):** se mantienen las mismas claves (`ink.900/800/700/600/400/300/100`, `pokeRed`) para no tener que tocar ningún call site, solo se recolorearon los valores — de gris-azulado casi negro a navy nocturno de mapa (`#0B1A1F` fondo, `#122530` superficie, `#E7D9B8` texto principal color pergamino). **Token nuevo:** `gold` (`#C9A46B` base, `400`/`600` para hover/pressed) — el trazo dorado del mapa, usado en bordes de tarjeta/botón/header en vez del gris neutro anterior. Los colores de tipo (`type.*`) y estado (`status.*`) NO se tocaron — son funcionales (indican tipo/estado real), no decorativos.
- **Tipografía (`src/theme/fonts.ts` + `app/_layout.tsx`):** se instaló `@expo-google-fonts/playfair-display` + `expo-font`, cargando solo 2 pesos (bold + bold italic) vía `useFonts()` en el layout raíz (pantalla en blanco brevísima mientras carga, sin splash screen dedicado). Aplicado NARROW — solo títulos de pantalla (`Header`) y títulos de tarjeta (home), nunca en texto de cuerpo — vía `style={{fontFamily: displayFont.regular}}` porque NativeWind no puede referenciar una fuente custom cargada a través de `className`.
- **Componentes compartidos actualizados** (se propagan solos a las ~20 pantallas que ya los reutilizan): `Header.tsx` (borde inferior dorado + título serif — confirma que el toggle de idioma y el repaletado llegan a toda pantalla que use `Header`, no solo a home), `Card.tsx` (borde dorado en vez de gris), `Button.tsx` (variante `secondary` con borde dorado).
- **Home (`app/index.tsx`) rediseñada como "sendero" del mapa:** línea vertical de puntos dorada (`borderStyle:'dashed'`) detrás de las tarjetas, con una chincheta roja (círculo de 9px) por tarjeta marcando cada "parada". El icono circular relleno de cada tarjeta se sustituyó por un marco de diamante (cuadrado rotado 45°, borde dorado, icono contrarrotado dentro) — motivo "marcador de mapa" en vez de badge de icono genérico. Extraído `WaypointIcon`/`Waypoint` como sub-componentes reutilizados por las 7 tarjetas para no repetir el marcado 7 veces.
- Verificado en el preview (inspección de estilos computados vía DOM, no capturas de pantalla — `computer` screenshot falla por timeout en esta sesión, entorno): fondo `rgb(11,26,31)` = `#0B1A1F` en home y en Pokédex (confirma propagación entre pantallas), título con `font-family: PlayfairDisplay_700Bold` y color `rgb(231,217,184)` = `#E7D9B8`, 7 tarjetas con borde `rgb(201,164,107)` = `#C9A46B`, línea de puntos y 7 chinchetas rojas de 9×9px presentes.
- **Alcance de esta pasada:** cubre el sistema compartido (tema, tipografía, Header, Card, Button) + la home como pantalla insignia. El resto de pantallas heredan el repaletado de fondo/borde/texto automáticamente por usar los mismos componentes, pero no se ha rediseñado cada pantalla individualmente con el motivo de "sendero"/chincheta — eso queda como pulido bajo demanda si Ferran quiere el mismo tratamiento en pantallas específicas (Pokédex, Guía, etc.) más adelante.

**[COMPLETO 2026-07-11] Resaltado de objetos rediseñado para funcionar en ambos idiomas — antes solo funcionaba en inglés.** Ferran reportó dos problemas reales sobre una captura de Mt. Moon en español: (1) el texto decía "Champiñón Diminuto" cuando el nombre oficial es "Miniseta", y (2) los objetos ocultos ya no eran pulsables para abrir la captura (se había perdido al añadir la traducción — el resaltado antiguo solo hacía pattern-matching contra "hidden X" en inglés). Pidió además que los chips de arriba ("Objetos en esta zona") dejaran de abrir imágenes (eso solo desde el texto resaltado) y que los objetos NO ocultos aparecieran en negrita dentro del texto.
- **Auditoría de traducciones antes de tocar el resaltado:** en vez de asumir que la traducción de ChatGPT estaba mal en general, se comprobó sistemáticamente cada nombre de objeto contra el diccionario oficial (`ITEM_NAMES_ES`) en las 4 regiones traducidas. Resultado: la inmensa mayoría de objetos (incluidas las 17 bayas con nombres inventados tipo "Baya Aranja") estaban traducidos correctamente — ChatGPT conocía bien los nombres oficiales de Pokémon. Solo ~10 nombres tenían una traducción real y consistente distinta a la oficial. **9 reemplazos de texto aplicados** en los 4 regiones (70 instancias + 24 de concordancia de género "un"→"una" para Miniseta): tinymushroom (Champiñón Diminuto→Miniseta), bigmushroom (→Seta Grande), nevermeltice (Antiderretir→Hielo Perpetuo), soothebell (Campana Alivio→Cascabel Alivio), shellbell (Campana Concha→Cascabel Concha), healpowder (Polvo Cura→Polvo Curación), itemfinder (Buscaobjetos→Zahorí, aunque en otros sitios ya usaba correctamente "Zahorí"), dragonfang/silkscarf (faltaba la preposición "de"), fósil Helix→Hélix (falta de tilde). Además se corrigió `metalcoat` en el propio diccionario (`Revest. Metálico`→`Revestimiento Metálico`, ya que ChatGPT usó consistentemente la forma completa y es más natural). **De paso, se encontró y arregló un bug preexistente no relacionado con esta sesión**: `move-names-es.json` tenía la traducción de "Psychic" guardada bajo la clave `"Psychic (move)"` en vez de `"Psychic"` (desambiguación de PokéAPI entre el tipo y el movimiento), lo que hacía que cualquier referencia a ese movimiento en español mostrara "Psychic" en inglés en toda la app, no solo en la Guía.
- **Rediseño de `renderLineWithItemHighlights`** (antes `renderLineWithHiddenHighlights`) en `app/guia/[region]/index.tsx`: ya no depende de encontrar la palabra "hidden" en el texto (ese marcador no existe de forma fiable en español — "oculto" aparece DESPUÉS del objeto, no antes, y con concordancia de género variable). Ahora usa el flag estructurado `item.hidden` de los datos directamente: busca la mención de cada objeto por su nombre de búsqueda (slug en bruto para inglés, nombre oficial en español para español, vía la nueva función `getItemSearchName`), y estiliza según el flag — objeto oculto: coloreado+subrayado+pulsable (abre la imagen), objeto visible: solo negrita. Una cola FIFO por nombre resuelve el caso de que el mismo objeto aparezca varias veces en un tramo.
- **`getItemSearchName` (nuevo, en `guideItemNames.ts`)**: para objetos MT/MO, genera una clave de búsqueda con guion simple y nombre de movimiento CON espacio ("MT-Pistola Agua"), distinta del formato bonito con raya larga que usa `prettifyItemName` para mostrar ("MT — Pistola Agua") — necesario porque el texto real de la guía (tanto en inglés como la traducción) usa el primer formato, nunca el segundo.
- **Bug de límites de palabra encontrado y arreglado durante la verificación**: `\b` de JavaScript es solo ASCII (`[A-Za-z0-9_]`) y nunca reconoce un límite junto a una letra acentuada — "Éter" nunca habría coincidido con el patrón `\b(...)\b`. Sustituido por una comprobación manual de los caracteres antes/después contra una clase `[a-zA-ZÀ-ÿ]` (cubre Latin-1 Supplement), sin usar lookbehind para máxima compatibilidad con el motor Hermes de React Native.
- **`ItemsRow` ahora es una lista puramente informativa** (ya no usa `PressScale`, ya no abre imágenes) — la única forma de ver una captura es pulsar la palabra resaltada en el texto.
- **Verificación:** con el clasificador de seguridad del navegador temporalmente caído durante esta sesión (no se pudo abrir el preview real), se verificó exhaustivamente por otras vías: TypeScript compila sin errores (`npx tsc --noEmit`), y se replicó el algoritmo de resaltado completo en Node contra los datos reales de las 4 regiones traducidas — **91,1% de los 1882 objetos coinciden correctamente** con su mención en el texto (el resto son casos sueltos de un objeto concreto con una redacción distinta a la oficial, degradación segura: simplemente no se resalta esa mención concreta, no rompe nada). **Confirmado visualmente en el preview real** en la sesión siguiente (navegador ya disponible): click en "Miniseta" abre el modal con la captura correcta sin colapsar la tarjeta, "Poción"/"MT-Pistola Agua" en negrita sin subrayado ni click, y los chips de `ItemsRow` ya no responden a click (verificado con un click real, no solo inspección de estilos).

**[COMPLETO 2026-07-11] Oráculo: el campo de pregunta ya se vacía tras responder.** Encontrado durante la verificación manual de la sesión anterior (me pasó a mí mismo varias veces probando preguntas seguidas): `runQuery` en `OracleButton.tsx` guardaba el texto preguntado en el propio input (`setQuery(text)`) en vez de limpiarlo, así que si el usuario escribía una segunda pregunta sin borrar antes a mano, el texto nuevo se concatenaba con el de la pregunta anterior en vez de reemplazarlo. Cambiado a `setQuery('')` tras calcular la respuesta — verificado en el preview con dos preguntas seguidas sin borrar manualmente, el campo queda vacío y listo para la siguiente cada vez.

**[COMPLETO 2026-07-11] Oráculo (`?`) hecho realmente bilingüe — antes solo funcionaba en español pese a recibir el `locale` como parámetro.** Ferran pidió "adelante con todo" sobre la lista de sueltos pendientes; al localizar primero el índice de objetos (`src/lib/oracle/items.ts`, cacheado a nivel de módulo sin locale — limitación ya documentada) se encontraron dos bugs reales más al verificar en inglés:
- **Los 8 regex de detección de intención (`RE_LOCATION`, `RE_TM_HM`, `RE_EFFECT`, `RE_ABILITY_HOLDERS`, `RE_STATS`, `RE_TYPE_OF`, `RE_EVOLUTION`, `RE_EFFECTIVENESS`) en `intents.ts` solo reconocían palabras clave en español** ("donde", "que hace", "tipo"...) — con la app en inglés, prácticamente ninguna pregunta con palabra clave activaba una intención (solo el lookup directo por nombre de Pokémon seguía funcionando, vía el fallback incondicional al final de la rama 6). Arreglado añadiendo alternativas en inglés a cada regex (where/find/catch/get/buy, what does/effect of, which pokemon/who has, weak/resist/etc.).
- **Todo el texto de las respuestas estaba además escrito directamente en español dentro del código** ("Débil x2 a:", "Evoluciona de:", "Tipo:", "(oculto)"...), ignorando el `locale` recibido — solo los VALORES (nombres de Pokémon/movimiento/habilidad/ubicación/objeto) ya usaban las funciones `localized*` existentes. Arreglado añadiendo ~26 claves nuevas bajo `oracle.answers.*` en `es.json`/`en.json` (interpolación `%{var}` de i18n-js) y reescribiendo cada función de `intents.ts` para usar `t('oracle.answers.X', {..., locale})` en vez de template strings fijos.
- **De paso, tercer bug de la misma familia**: `region.nameEs` se usaba sin condicionar en el índice de objetos (Teselia aparecía como "Teselia" incluso en inglés, donde debería ser "Unova") y en `TUTOR_REGION_LABEL` de `intents.ts` (mismo problema, ámbito TM/MO/tutor). Ambos arreglados para elegir `nameEn`/`nameEs` según `locale`.
- **Alcance deliberadamente NO tocado**: el propio dataset de encuentros salvajes (`locations.json`, campos `location`/`locationType`/`rate` tipo "Route 1"/"Tall Grass"/"Very Rare") sigue en inglés en ambos idiomas — son datos crudos de la wiki, no texto de plantilla, y traducirlos es una tarea de dataset separada y mucho más grande, no la que se pidió aquí.
- Verificado en el preview en ambos idiomas con 5 tipos de pregunta (ubicación de objeto oculto, ubicación de Pokémon salvaje, stats, debilidades/resistencias, ida y vuelta ES↔EN sin recargar) — todas las plantillas, nombres de región y "(oculto)"/"(hidden)" cambian correctamente con el idioma.

**[COMPLETO 2026-07-11] Johto ya tiene instrucciones de ruta — reescritas desde cero, no copiadas.** Ferran pidió literalmente copiar el texto de la guía original de CommanderBlue ("como no vamos a lucrar, vamos a copiar eso"). Se le explicó que la falta de monetización no elimina el riesgo de copyright (el propio `CLAUDE.md` ya documenta que Nintendo ha tumbado proyectos de fans sin ánimo de lucro — Pokemon Essentials, Relic Castle, Pokenet — y que aquí ni siquiera es el IP de Nintendo lo que estaría en juego, sino el texto original del autor del blog) y que citar la fuente tampoco lo resuelve (atribución evita el plagio, no el problema de copyright). Ferran aceptó la alternativa ya usada en otras partes del proyecto (construcción de equipos, FAQ de legendarios errantes): extraer los hechos y redactarlos desde cero.
- **Proceso:** se descargó el HTML original (`curl`, no `WebFetch`) y se leyó completo (~700 líneas de prosa narrativa en primera persona). Se extrajeron los hechos (objetos, ruta, eventos de la trama, jefes de gimnasio) de las 14 secciones y se redactaron desde cero en formato mecánico tipo "ve al norte y coge X", igual que las otras 4 regiones — nunca copiando frases de CommanderBlue. Escrito directamente en inglés y español a la vez (solo 14 tramos, tamaño manejable sin necesidad de delegar a ChatGPT esta vez), con el mismo criterio ya establecido: nombres de lugar en inglés dentro del texto en español.
- **Nota sobre los límites de tramo:** la guía original tiene más subtítulos narrativos (18) que los 14 `location` ya fijados en `johto.json` de una extracción anterior de esta sesión (p. ej. "ON THE WAY TO THE LIGHTHOUSE AND THE SWIRLS" o "THE ROCKETS RETURN: RADIO TOWER INVASION" no tienen tramo propio). Para no romper la estructura ya construida (traducciones de título, objetos ya extraídos), el contenido de esas secciones "huérfanas" se plegó dentro del tramo de los 14 ya existentes al que pertenece temáticamente (confirmado cruzando contra qué objetos ya estaban asignados a cada tramo — encajaban exactamente con este criterio, p. ej. "MAHOGANY AND THE LAKE OF RAGE" ya tenía "Card Key"/"HM5" que en realidad vienen de la sub-trama del Radio Tower).
- **`data/guides/johto.json`**: los 14 tramos ahora tienen `steps`/`stepsEs` poblados (antes vacíos), campo `note` actualizado explicando la decisión. Verificado en el preview en ambos idiomas — el tramo más largo y complejo (12, la trama completa del Team Rocket) se muestra íntegro y en orden correcto tanto en inglés como en español.

**[BUG ARREGLADO 2026-07-11] `getLocationImage` mostraba la captura de una ubicación equivocada.** Reportado por Ferran ("miro la img del bosque verde y me sale otro pueblo"). Causa real: el fallback de coincidencia por palabra clave (pensado solo para los títulos narrativos compuestos de Johto, tipo "AZALEA AND KURT'S HOUSE") se aplicaba a las 5 regiones por igual, y producía falsos positivos de dos tipos en Kanto/Hoenn/Sinnoh/Teselia (auditado con un script, 31 casos): (1) lugares distintos que comparten una palabra — "VIRIDIAN FOREST" coincidía con la imagen de "Viridian City", "CERULEAN CAVE" con "Cerulean City", "PETALBURG WOODS" con "Petalburg City", etc.; (2) colisión de substring en números de ruta — "ROUTE 25" coincidía con "Kanto Route 2" porque la cadena "ROUTE 25" contiene literalmente "ROUTE 2". **Arreglo:** el fallback por palabra clave ahora solo se ejecuta si `region === 'johto'` — para las otras 4 regiones, si no hay coincidencia EXACTA no se muestra ninguna imagen en vez de adivinar. Verificado en el preview: "Bosque Verde"/Viridian Forest y "Route 25" ya no muestran imagen (antes mostraban la incorrecta), mientras que coincidencias exactas reales (Route 1/10/11/13/15/16) se siguen mostrando bien.

**[COMPLETO 2026-07-11 — 5 de 5 regiones] Nombres de ubicación de la Guía traducidos a los nombres oficiales en español.** Ferran, avisado explícitamente de que esto contradice cómo funciona PokeMMO de verdad (el juego NO traduce los nombres de lugar — ver la regla ya documentada en la sección de Johto/Kanto más abajo), confirmó que lo quería igualmente. Con el aviso entendido y aceptado, se implementó como capa de visualización, nunca tocando el dato real.
- **Fuente:** delegado a ChatGPT (PokéAPI resultó muy floja para esto — solo 2/81 nombres de Sinnoh tenían traducción española en `location_names.csv`, a diferencia de los objetos que sí tenían cobertura excelente en la tabla equivalente). Se le pidió el nombre oficial exacto según el juego base real de cada región (Rojo Fuego/Corazón de Oro/Esmeralda/Diamante-Perla/Blanco-Negro, mismos ROMs ya documentados en "Alcance del juego" de este archivo) — validado con un script que compara claves 1:1 contra la lista original antes de aplicar nada: 0 discrepancias en las 4 regiones (73/65/81/60 nombres). Muestreo de calidad (Kanto, Sinnoh) confirmó nombres correctos ("Pueblo Paleta", "Ciudad Celeste", "Monte Corona", "Ciudad Jubileo"...).
- **`data/reference/location-names-es.json`** (257 entradas, las 4 regiones fusionadas en un único mapa plano — sin conflictos entre regiones que comparten nombre, p. ej. "SAFARI ZONE"/"ROUTE N" traducen igual en todas). `localizedLocationName(location, locale)` en `src/lib/guides.ts`, mismo patrón que `localizedMoveName()`/`localizedAbilityName()` de `pokedex.ts`: el campo `location` real (usado para `getLocationImage`, búsqueda interna) nunca se toca, solo se traduce lo que se pinta en pantalla.
- Conectado en `app/guia/[region]/index.tsx`: título de cada tarjeta de tramo + filtro de búsqueda (ahora encuentra tanto por nombre en inglés como por el traducido). Verificado en el preview: Kanto/Sinnoh muestran los nombres oficiales en ES y revierten correctamente al original en EN sin regresión.
- **Johto completado en un segundo lote** (14 títulos de capítulo compuestos, prompt distinto al de las otras 4 regiones — traducción natural del título pero manteniendo el nombre propio de la ciudad/zona en inglés dentro de la frase, p. ej. "NEW BARK, THE BEGINNING OF THE JOURNEY" → "NEW BARK, EL COMIENZO DEL VIAJE"). Validado 14/14 sin discrepancias antes de fusionar. `data/reference/location-names-es.json` queda en 271 entradas totales (257 de las 4 regiones "simples" + 14 de Johto). Verificado en el preview: las 14 filas de Johto muestran el título traducido con el nombre propio intacto en inglés.

**[COMPLETO 2026-07-11] Diccionario de objetos de la Guía traducido al español.** Cierra la limitación conocida del punto anterior (los chips de `ItemsRow` mostraban nombre en inglés aunque la frase ya estuviera en español). A diferencia de los nombres de ubicación (ver más abajo, mucho más flojos en PokéAPI), los ITEMS sí tienen cobertura excelente en la misma fuente ya usada para `move-names-es.json`/`ability-names-es.json` (PokéAPI `item_names.csv`, language_id=7=es-ES): 1539 pares EN/ES verificados, coincidencia de 233/251 nombres del diccionario del proyecto de forma automática (normalizando y cruzando por nombre en inglés), los 18 restantes (objetos clave sin traducción en esa tabla — Pokégear, Pokédex, C-Gear, etc.) rellenados a mano con el término oficial conocido.
- **`src/lib/guideItemNames.ts`**: nuevo diccionario `ITEM_NAMES_ES` (mismas claves que `ITEM_NAMES`, solo cambia el valor mostrado) + `prettifyItemName(raw, locale)` ahora acepta el locale — traduce también el prefijo de MT/MO ("TM —"/"HM —" → "MT —"/"MO —", abreviaturas reales del juego en español) y el nombre del movimiento vía `move-names-es.json` ya existente. Verificado con Mt. Moon: "TM-WaterGun" → "MT — Pistola Agua", "tinymushroom" → "Miniseta", "moonstone" → "Piedra Lunar", coincide exactamente.
- **Conectado** en `app/guia/[region]/index.tsx` (`ItemsRow` + filtro de búsqueda) vía `useLocaleStore`. **NO conectado** en `src/lib/oracle/items.ts` (el índice de objetos del asistente "?") — ese índice se construye una sola vez y cachea a nivel de módulo sin parámetro de idioma; hacerlo bilingüe requeriría cachear por locale, no se ha tocado en esta pasada (sigue en inglés, sin regresión, simplemente no traducido).

**[COMPLETO 2026-07-10] Guías de región disponibles en español — Ferran cambió de idea sobre una decisión anterior de esta misma sesión.** Tras ver el toggle de idioma en uso real, Ferran pidió expresamente que el contenido de las guías de walkthrough SÍ esté en español (revocando la decisión de sesiones previas de dejarlo siempre en inglés). Volumen grande (~228.000 caracteres en Kanto/Hoenn/Sinnoh/Teselia — Johto no tiene texto narrativo, ver nota de Johto más arriba, nada que traducir ahí), delegado a ChatGPT.
- **Antes de traducir, se resolvió un problema de arquitectura real:** el resaltado clicable de "objeto oculto" (`renderLineWithHiddenHighlights` en `app/guia/[region]/index.tsx`) depende de un regex que busca la palabra literal "hidden" seguida del slug en inglés del objeto dentro de la frase — traducir el texto rompía silenciosamente el único punto de acceso a las capturas de Gyazo de objetos ocultos. Se añadió `ItemsRow`, una fila "Objetos en esta zona" con chips de TODOS los objetos del tramo (no solo los ocultos), independiente del idioma, pulsable para ver la captura — mejora también la versión en inglés (antes solo se podía encontrar un objeto oculto localizando la palabra resaltada dentro del texto).
- **Tipo:** `GuideStep.stepsEs?: string[]` en `src/lib/guides.ts` — array paralelo a `steps`, mismo número de líneas/orden. Opcional: si no existe (o el locale no es `es`), se sigue mostrando `steps` en inglés con el resaltado de siempre sin cambios.
- **Proceso de traducción:** se exportó un JSON por región (`{order, location, steps}`, sin `items` — no hacía falta) a un prompt para ChatGPT con instrucciones explícitas: español de España (nunca LatAm), nombres de lugar SIEMPRE en inglés dentro de la frase traducida (PokeMMO no los localiza), nombres de objeto sí traducidos con naturalidad, mismo número exacto de tramos y de frases por tramo (para poder reemparejar por posición sin ambigüedad).
- **Validación antes de aplicar:** script que compara recuento de tramos + `order` + nº de líneas por tramo entre el original y la traducción — 0 discrepancias en las 4 regiones (101/108/150/88 tramos). Muestreo de calidad (Kanto, Mt. Moon) confirmó nombres de lugar intactos en inglés ("ve al norte hacia Viridian City"), objetos traducidos con naturalidad ("Champiñón Diminuto", "Piedra Lunar", "MT-Pistola Agua"), tuteo correcto.
- Verificado en el preview: Mt. Moon en ES muestra el texto traducido completo + la fila de 20 objetos (con "· Oculto" en los que corresponde); cambiando a EN se recupera el texto original con el resaltado clicable de siempre, sin regresión.
- **Limitación conocida, no resuelta en esta pasada:** los chips de `ItemsRow` muestran el nombre del objeto vía `prettifyItemName()`, que solo tiene nombres en inglés (p. ej. "Tiny Mushroom" en vez de "Champiñón Diminuto") — la frase narrativa sí está en español pero los chips de objeto no, una inconsistencia menor. Traducir ese diccionario es una tarea separada y más pequeña, no pedida explícitamente esta vez.

**[COMPLETO 2026-07-10] Sendero + chincheta extendido a la sección Guía.** Ferran confirmó extender el motivo más allá de la home. Se aplicó deliberadamente solo donde tiene sentido semántico real (evitando decoración sin significado, ver principio "structure is information") — NO se aplicó a listas largas de datos como la Pokédex (649 filas) o el listado de torneos, donde una línea de "ruta" no representa nada real:
- `app/guia/[region]/index.tsx` (`LocationCard`): es el ajuste más natural de todos — el listado de ubicaciones YA es, literalmente, la ruta de la partida en orden de visita. Cada fila dibuja su propio segmento de línea punteada dorada (en vez de una única línea absoluta sobre todo el `FlatList`, fràgil de medir con virtualización) que conecta visualmente con la siguiente fila; el badge circular de número de tramo pasó de gris a rojo (chincheta "estás aquí" + número); nombre de ubicación en serif.
- `app/guia/index.tsx`: rejilla de regiones con marco dorado (sin sendero — es una rejilla de 2 columnas, no una secuencia lineal, así que el sendero no encajaba ahí); lista de 10 guías de referencia con el mismo patrón sendero+chincheta que la home.
- Verificado en el preview (conteo de elementos vía DOM): 10 segmentos punteados + 10 chinchetas rojas en Kanto (primera página del `FlatList`) y en el hub de Guía, título de ubicación con `font-family: PlayfairDisplay_700Bold` confirmado.

**[COMPLETO 2026-07-10] Capturas de ubicación en la Guía — Idea 2 cerrada.** ChatGPT devolvió `pokemmo_location_images.json` (125 ubicaciones con imagen verificada en las 5 regiones + 69 marcadas `unverified` por bloqueo anti-bot de la wiki durante la recopilación) — capturas propias de PokeMMO alojadas en `images.shoutwiki.com`, no del juego oficial ni de terceros, coherente con la decisión ya tomada de evitar el riesgo de IP del "Detonado" de Sinnoh.
- **Construido:** `data/guides/location-images.json` (el JSON de ChatGPT tal cual, con `source`/`note` añadidos). `getLocationImage(region, tramoLocation)` en `src/lib/guides.ts`: primero intenta coincidencia exacta quitando el prefijo de región del nombre de la wiki ("Kanto Route 1"→"ROUTE 1", cubre la convención simple de Kanto/Hoenn/Sinnoh/Teselia), y si no encuentra nada cae a una coincidencia por palabra clave (nombre de ciudad sin "Town"/"City" como substring) — necesario porque Johto no usa nombres de lugar simples en `location`, sino títulos de capítulo tipo "NEW BARK, THE BEGINNING OF THE JOURNEY".
- `app/guia/[region]/index.tsx`: cada tarjeta de tramo expandida muestra la captura (si hay coincidencia) encima del texto, pulsable para verla a pantalla completa. Se generalizó `ImageViewerModal` (antes solo aceptaba una URL de página de Gyazo y derivaba la imagen directa) para aceptar `{direct, fallback}` — las capturas de Gyazo de objetos siguen derivando la URL directa vía `gyazoDirectImageUrl()`, las nuevas capturas de ubicación ya son URL directa de por sí, sin necesidad de derivar nada.
- Verificado en el preview (vía inspección del DOM, no capturas de pantalla — el `computer` screenshot está fallando por timeout en esta sesión): Kanto Route 1/10/11/13/15/16 cargan la imagen correcta (`naturalWidth` > 0, sin bloqueo de hotlink), y Johto "New Bark Town" resuelve correctamente por el fallback de palabra clave contra el título compuesto del capítulo.
- **69 ubicaciones sin imagen verificada** (bloqueo anti-bot durante la recopilación) simplemente no muestran captura — no se ha reintentado.

**[COMPLETO 2026-07-10] Cobertura de objetos mejorada en las guías de walkthrough (Kanto/Hoenn/Sinnoh/Teselia).** Ferran pidió retomar el hueco conocido de "cobertura de objetos parcial" (parseo heurístico original por regex, no perfecto). En vez de re-scrapear las páginas fuente, se aprovechó que el texto narrativo (`steps`) YA está guardado tal cual en cada región — se escaneó ese texto ya existente contra el diccionario `ITEM_NAMES` de `guideItemNames.ts` (ya bastante más grande que cuando se hizo la extracción original) buscando nombres de objeto que aparecen en la prosa pero no en la lista `items` de ese mismo tramo.
- El escaneo automático dio 53 candidatos en las 4 regiones (Kanto 8, Hoenn 9, Sinnoh 11, Teselia 25) — **NO se aplicaron en bloque**: se revisó cada uno a mano con su contexto de texto, porque el escaneo por substring produce falsos positivos reales, confirmados con ejemplos concretos: "revive" apareciendo en frases sobre la máquina de revivir fósiles/rituales de estatua (no un objeto Revive real), "iron" coincidiendo con "Iron Island" (nombre de lugar, no la vitamina Iron), "pokeball" en frases tipo "pick a eevee in a pokeball" (describe el Pokémon regalo, no un objeto extra), y un caso de elección de diálogo en Striaton City donde "antidote"/"awakening" son palabras que se dicen en una conversación para conseguir un Full Heal (que sí estaba ya capturado), no objetos recibidos en sí.
- **42 de los 53 candidatos eran objetos reales que faltaban** — aplicados directamente a los JSON de datos (`data/guides/{kanto,hoenn,sinnoh,teselia}.json`): incluye elecciones de fósil/baya no capturadas antes (p. ej. Mt. Moon de Kanto solo tenía `domefossil`, le faltaba `helixfossil` como alternativa; Castelia City de Teselia tenía toda una interacción de un NPC payaso con 4 bayas a elegir de las que solo se había capturado 1, `pechaberry` — se añadieron `cheriberry`/`chestoberry`/`rawstberry`/`aspearberry`). Verificado en el preview: el badge de nº de objetos de Castelia City pasó de 15 a 20, coincide exactamente con lo esperado.
- 11 candidatos se descartaron explícitamente por ser falsos positivos (documentados arriba) — no se tocaron esos tramos.
- **Johto queda fuera de esta pasada a propósito**: no tiene texto narrativo guardado (decisión ya tomada antes en la sesión, ver nota de Johto más abajo), así que este método de "escanear la prosa ya guardada" no aplica — mejorar su cobertura requeriría volver a la fuente HTML original, un trabajo distinto y más pesado, no hecho en esta pasada.

**[COMPLETO 2026-07-10] Planificador de cría multi-generación — hallazgo importante, cambia una decisión anterior de esta misma sesión.** Ferran pidió retomar el planificador que se había descartado antes por "no hay fórmula genérica verificable". Al releer el hilo del foro (`forums.pokemmo.com/topic/49440`) más a fondo esta vez (con `curl` en vez de `WebFetch`, ya que el foro sigue bloqueando `WebFetch` con 403), se encontró la sección "PART 1 - Perfectionists" que SÍ documenta un algoritmo genérico completo y verificable: un árbol binario de "doblado" — para llegar a un Nx31, combina dos (N-1)x31 que comparten N-2 IVs y difieren en 2 (esas 2 se fuerzan con brace). Confirmado con los números exactos del propio hilo: 32 Pokémon base 1x31 para un 6x31 completo, ratio "1:5:10:10:5:1" por stat, 62 braces (620.000 PokéYen) — todo esto coincide EXACTAMENTE con la construcción recursiva implementada (verificado antes de tocar la UI con una simulación en Node, ver `src/lib/breedingPlanner.ts`).
- **Construido:** `src/lib/breedingPlanner.ts` (`computeMultiGenPlan(targetStats)`, construye el árbol recursivamente: cada nodo combina dos sub-árboles de ventanas contiguas de stats objetivo, braceando las 2 IVs que no se solapan — genera automáticamente cuántos 1x31 de cada stat hacen falta, el nº total de cruces/braces, y el árbol completo generación a generación). El coste en PokéYen NO se recalcula — se reutiliza directamente la `costTable` ya verificada de `breeding.json` por nº de stats objetivo (con/sin naturaleza), ya que el coste es simétrico (no depende de qué stats concretas, solo de cuántas) según la propia mecánica.
- **UI:** `app/cria/index.tsx` ahora tiene un toggle "Un cruce" / "Planificador multi-generación". El modo planificador deja elegir 2-6 stats objetivo + toggle de naturaleza, y muestra coste, total de 1x31 base necesarios (desglosado por stat), y el árbol de cruces completo generación por generación (qué se combina con qué, braceando qué 2 IVs).
- Verificado en el preview: selección por defecto (PS+Atk+Def+SpA+SpD, 5 stats) → coste 480.000 sin naturaleza / 855.000 con naturaleza (coincide EXACTO con la tabla del foro), 16 base parents, 15 cruces, 30 braces, distribución 1/4/6/4/1 (fila 4 del triángulo de Pascal). Árbol completo de 4 generaciones, terminando en "Generación 4 — 1 cruce" (singular corregido tras detectar el error de gramática "1 cruces" en la primera pasada).
- **Nota de proceso:** esto corrige una conclusión anterior de esta misma sesión (ver más abajo, sección de cría/Fase 3) que decía que esto NO se podía construir sin inventar una fórmula. La investigación anterior solo había capturado la tabla de costes final del hilo, no esta sección más profunda con el procedimiento paso a paso — lección: cuando un hilo largo se marca como "sin fórmula genérica", vale la pena releerlo completo antes de descartar la idea definitivamente.

**[COMPLETO 2026-07-10] Guía "PokeMMO vs. oficial: objetos" — primera mitad de la Idea 1 de diferencias.** Ferran pidió una sección de diferencias PokeMMO-vs-oficial (ej. Mono Amuleto consumible en PokeMMO, no en el oficial), con instrucción explícita de delegar a ChatGPT cualquier parte que fuera scraping pesado. Investigación previa confirmó que el diff objeto-por-objeto (46 objetos con comportamiento distinto encontrados) requiere revisar la página individual de cada objeto en la wiki — tarea pesada, delegada a ChatGPT con un prompt estructurado (JSON con nameEs/nameEn/pokemmoEffect/officialEffect/differenceType/sourceUrl/confidence). ChatGPT devolvió `pokemmo_item_differences.json` (46 entradas) que se integró tal cual, sin reverificar cada fuente una por una (mismo nivel de confianza que cualquier dato con `source` documentado, no re-auditado campo a campo).
- **Construido:** `data/guides/differences.json` (nuevo, tipo `DifferencesGuide`/`DifferenceItem` en `src/lib/guides.ts`, entrada `differences` en `ReferenceTopic`/`REFERENCE_TOPICS`/`getReferenceGuide()`) + render `DifferencesBody` en `app/guia/referencia/[topic].tsx` (nombre ES/EN, badge de tipo de diferencia, efecto en PokeMMO vs. efecto oficial, nivel de confianza) + claves i18n nuevas (`guide.pokemmoEffectLabel`/`officialEffectLabel`/`confidenceLabel`/`topics.differences`, ES y EN). No hizo falta tocar `app/guia/index.tsx` — la lista de guías de referencia se genera sola desde `REFERENCE_TOPICS`.
- Verificado en el preview: la nueva entrada aparece en el índice de Guía y la pantalla de detalle renderiza las 46 diferencias sin errores.
- **Pendiente — segunda mitad de la Idea 1:** una guía de mecánicas generales (cría, tasa shiny, Pokémon Alfa, movepools Gen 8, etc.) a partir de un hilo del foro ya investigado — más barata, no delegada a ChatGPT, sin construir aún porque Ferran no la seleccionó explícitamente al decidir cómo seguir.
- **Pendiente — Idea 2 (imágenes por ubicación):** segundo prompt para ChatGPT ya preparado y enviado (capturas self-hosted de la wiki por ciudad/ruta en las 5 regiones), resultado aún no recibido.

**[COMPLETO 2026-07-10] Fase 5, calculadora de daño — CIERRA EL ROADMAP ORIGINAL DE 5 FASES.** `src/lib/stats.ts` (fórmula de stat real Gen 3+: `computeHp`/`computeOtherStat`/`computeStat` a partir de base+IV+EV+nivel+naturaleza, más `stageMultiplier`/`applyStage` para las fases de combate -6 a +6) + `src/lib/damageCalc.ts` (`computeDamageRange()`, fórmula de daño de Gen 5 — **crítico x2, no x1.5 de Gen 6+**, verificado que es la fórmula correcta para el motor de combate de PokeMMO ya que el juego se basa explícitamente en mecánicas Gen 1-5 en las 5 regiones). `app/danio/index.tsx`: atacante y defensor completos (Pokémon real, nivel, naturaleza, EV/IV del stat relevante, fase de combate) + selector de movimiento limitado a los movimientos de daño reales del atacante (`getMoveset`, category≠status) + toggles de crítico/quemadura + campo de "otro multiplicador" para clima/objetos no modelados → rango de daño mín-máx y % de la vida del defensor, con STAB y efectividad de tipo detectados automáticamente.
- **Alcance deliberadamente acotado, documentado en la propia UI**: cubre la fórmula base (nivel/poder/stats/fase/crítico/tirada aleatoria/STAB/efectividad de tipo/quemadura) pero NO modela clima, movimientos multigolpe, reducción por combate doble, ni objetos/habilidades específicos (Life Orb, Choice, Technician...) — cada uno necesitaría su propia verificación específica de PokeMMO por separado. El campo "otro multiplicador" deja que el usuario que sepa que aplica alguno lo introduzca sin que la app finja saber algo que no ha verificado.
- **Verificado a mano, no solo en el preview**: Charizard Nv.50 (neutro, IV31/EV0) usando Ascuas (40 poder, Fuego) contra Bulbasaur Nv.50 (neutro, IV31/EV0) → calculado en papel con la fórmula paso a paso: base=28, STAB x1.5, efectividad x2 (Fuego vs Planta/Veneno), tirada 85-100% → rango esperado 68-84 de daño, 56%-70% de la vida (PS máx. Bulbasaur=120). La calculadora dio exactamente 68–84 / 56%–70%, coincidencia exacta.
- Con esto se cierran las 5 fases del roadmap original (`CLAUDE.md` sección "Features, en orden de construcción"), y además la V3 del creador de equipos, el rol de combate heurístico y la tabla de coste de cría (todo completado la misma sesión, 2026-07-10). Quedan sueltos menores sin fuente de datos disponible (TMs/HMs de Johto/Teselia, cobertura de objetos de guías a pulir bajo demanda) y un planificador de cría multi-generación deliberadamente descartado por falta de una fórmula genérica verificable — nada bloqueante, todo son refinamientos opcionales o genuinamente imposibles con las fuentes actuales.

**[COMPLETO 2026-07-10] Fase 4, tracker de shinies con widget en home.** `src/store/shinyStore.ts` (zustand+persist, mismo patrón que `tournamentStore.ts`/`localeStore.ts`): contador manual de encuentros por "caza" (`ShinyHunt {id, pokemonId, count, startedAt, caughtAt}`), una caza activa a la vez (`activeHuntId`), sin detección automática de nada — el usuario le da a +1 él mismo cada vez que le sale el Pokémon en el juego, coherente con la regla del proyecto de cero notificaciones/vigilancia. `app/shinies/index.tsx`: buscador para iniciar caza nueva + lista de todas las cazas (activa primero) con +1/-1, marcar como capturada, cambiar cuál es la activa, eliminar (con confirmación `Alert.alert`, mismo patrón que borrar un torneo). **Widget en `app/index.tsx`** (lo que pedía explícitamente el roadmap, "widget pasivo en pantalla de inicio"): si hay una caza activa, aparece una tarjeta compacta arriba del todo (sprite + nombre + contador) con su propio botón +1 independiente — se puede sumar un encuentro sin salir de la home. Verificado en el preview: caza de Feebas, +1 tanto desde `/shinies` como desde el botón del widget en home, contador sincronizado entre ambos sitios.
- Con esto la Fase 4 queda completa (el Showdown export de más abajo se hizo primero en esta misma sesión).

**[COMPLETO 2026-07-10] Fase 4, exportar equipos a formato Showdown.** Extensión de `app/equipos/index.tsx`: cada miembro del equipo ahora puede tener un "set" completo (mote, objeto, habilidad, naturaleza, EVs, IVs, hasta 4 movimientos elegidos de su moveset real vía `getMoveset()`), editable en un modal (`BuildEditorModal`) abierto tocando su fila (el icono de lápiz hace lo mismo; el icono "x" sigue siendo el único que quita del equipo — antes tocar la fila entera quitaba al Pokémon, cambio de UX deliberado porque editar es la acción más probable). Botón "Exportar equipo" genera el texto en formato Pokémon Showdown (`src/lib/showdown.ts`, `exportTeamToShowdown()`) con copiar/compartir, mismo patrón que `torneos/[id]/export.tsx`. Nuevo `data/reference/natures.json` (25 naturalezas, boost/reduce por stat).
- **Bug real encontrado y arreglado durante la verificación (no solo un artefacto de prueba):** el modal pasaba el build COMPLETO ya calculado (`onChange(next: TeamMemberBuild)`) en vez de una función actualizadora. Si dos cambios de campo llegan a dispararse antes de que React re-renderice con el resultado del primero (probado disparando varios taps seguidos en la misma pasada de automatización), el segundo calcula su `next` a partir del mismo `build` "stale", y pisa silenciosamente el cambio del primero. Arreglado cambiando la firma a `onChange(updater: (prev) => TeamMemberBuild)`, enrutando cada campo por `setBuilds` con forma funcional — así cada actualización se aplica siempre sobre el último estado real, sin importar el orden/ritmo de disparo. **Patrón a replicar en cualquier futuro editor con modal + múltiples campos**: nunca pasar el objeto ya-computado hacia arriba, pasar siempre una función actualizadora.
- Verificado en el preview con un set completo (Charizard @ Choice Scarf, Blaze, Jolly, 252 Spe, Flare Blitz + Dragon Claw) → exportación exacta: `Char (Charizard) @ Choice Scarf` / `Ability: Blaze` / `EVs: 252 Spe` / `Jolly Nature` / `- Flare Blitz` / `- Dragon Claw`.
- **Pendiente si hace falta más adelante:** tracker de shinies con widget (resto de Fase 4) y calculadora de daño (Fase 5) — ver roadmap más abajo.

**[COMPLETO 2026-07-10] Fase 3, calculadora de cría.** Ferran se ausentó y autorizó seguir trabajando sin pedir permiso ("tira tu solo") — se continuó con lo siguiente del roadmap. **Hallazgo importante verificado con la guía del foro oficial** (`forums.pokemmo.com/topic/49440-the-new-breeding-guide`, autor Gilan, enlazada como referencia vigente desde el hilo fijado "Useful Links for Competitive Play"; bajada con `curl`, no `WebFetch`): **el sistema de cría de PokeMMO NO es el de los juegos oficiales** — nada de Piedra Lazo (Destiny Knot) ni IVs 0-31 aleatorias en los espacios no heredados. Es un sistema propio:
- **3 de los 6 IVs se heredan directamente** de cualquiera de los dos progenitores (al azar en cuál stat y de cuál progenitor); **las otras 3 se PROMEDIAN entre ambos progenitores, redondeando hacia abajo.**
- **"Braces"** (comprables a un PNJ de la guardería, 10.000 cada uno): fuerzan que un IV concreto se herede de ese progenitor. Cada progenitor solo puede llevar un objeto (brace O Everstone, nunca ambos) → como máximo 2 de las 3 IVs heredadas se pueden elegir a mano, la tercera siempre es al azar.
- **Everstone**: igual que en los juegos oficiales, 100% de probabilidad de heredar la naturaleza del progenitor que la lleva.
- **Habilidad**: 80% de probabilidad de heredar la de la madre (si tiene 2 posibles) — distinto de los juegos oficiales. Criar por habilidad no suele preocupar porque se puede cambiar después con un objeto consumible.
- **Cría de 2 shiny**: fórmula de IVs distinta (2 heredadas/forzables + 2 al "mejor de los 2 progenitores" + 2 promediadas) y la cría es shiny garantizado.
- Verificado contra varios de los ejemplos numéricos trabajados en el propio hilo (p. ej. 31/0/0/0/0/0 + 0/31/0/0/0/0 con brace en cada uno → 31/31/0/0/0/0) — la calculadora reproduce el resultado exacto.
- **Construido**: `data/guides/breeding.json` (guía de referencia, nueva entrada `breeding` en `REFERENCE_TOPICS`/`ReferenceTopic`/`getReferenceGuide()` de `guides.ts`, con mecánicas + tabla de coste de selección de sexo + fórmula de cría shiny) + `src/lib/breeding.ts` (`computeBreedingOutcome()`, puro cálculo) + `app/cria/index.tsx` (calculadora interactiva: IVs de cada progenitor, selector de brace por progenitor, toggle de Everstone, toggle de cría shiny → resultado por stat mostrando el valor garantizado o las 3-4 posibilidades según el grupo en que caiga cada stat sin brace). Tarjeta nueva en la home.
- **Lección de proceso repetida (tercera vez esta sesión, misma causa que `/equipos`):** al crear una carpeta de ruta nueva (`app/cria/`), el tipado de rutas de Expo Router (`router.d.ts`) no se regenera limpio hasta reiniciar el servidor de desarrollo — `npx tsc --noEmit` da un error confuso listando rutas de módulos internos (`/../src/lib/breeding`, etc.) hasta hacer `preview_stop`+`preview_start`. Si aparece este error específico al añadir una pantalla nueva, reiniciar el servidor antes de investigar más.

**[COMPLETO 2026-07-10 — mejor esfuerzo, marcado explícitamente como no verificado] Fase 3, tier list competitiva.** Investigación exhaustiva de las 4 fuentes de la jerarquía de la regla de oro: **ninguna tiene hoy una foto completa y actual de "qué Pokémon está en qué tier"** — el tiering de PokeMMO es un sistema vivo recalculado por uso en el ladder, no una lista estática. Detalle de lo investigado:
- **PokeMMO Wiki** (`/wiki/OU`, `/UU`, `/NU`): tablas HTML limpias y parseables (bajadas con `curl`, no `WebFetch`, para evitar truncado) — pero son listas de **elegibilidad acumulada**, no tier por Pokémon (OU=622 entradas, UU=578, NU=527 — la diferencia entre listas SÍ da el tier real: `uber` explícito, `ou`=OU∖UU, `uu`=UU∖NU, `nu`=NU). Un bug de la wiki (enlace roto en la fila de Farfetch'd, num #83, en las 3 tablas) hizo que el parser capturara "Defiant" en vez del nombre — corregido con un caso especial por número de dex, ya que Farfetch'd es un problema de la wiki ya documentado en `abilities.json`.
- **`/wiki/Uber`**: tabla vacía, solo una sección "History" en prosa que se corta el 24-03-2022 y con huecos (menciona quitar a Reshiram sin documentar cuándo se añadió). Última foto fiable: Uber = {Wobbuffet, Dugtrio, Rayquaza} (con Wobbuffet/Dugtrio y Rayquaza **presentes** en la tabla de elegibilidad OU de la wiki, en contradicción directa con el historial del foro — se resolvió dando prioridad al historial del foro, forzando estos 3 a `uber` aunque la tabla OU los incluyera).
- **Foro oficial, Competition Alley**: el hilo fijado "Tier Ban Lists" solo cubre casos especiales (Borderline/Borderline 2), no la lista completa. Los hilos mensuales "[Mes]-2026 Usage Movements" (hasta julio 2026) son solo diffs de 1-5 Pokémon por mes, con "discusión en Discord" — no una foto completa, y reconstruir sumando años de diffs sería inviable. El hilo "oficial" que en su día enlazaba estadísticas de uso en vivo (`topic/53984`) está borrado (404) — el detalle se mudó a Discord en algún momento.
- **TierMaker** ("Season 14"): bloqueó `WebFetch` (403) pero no `curl`; resultó ser un tablero tipo imagen/plantilla, sin forma clara de confirmar vigencia.
- **Decisión de Ferran (2026-07-10): construir igualmente con la mejor combinación disponible, con aviso explícito de "no verificado desde 2022" en la UI** (en vez de aparcarlo o pedirle que lo consulte en el juego). Implementado:
  - `data/tiers/pokemmo_tiers.json` (649 entradas): `tier` calculado por diferencia de conjuntos entre las tablas OU/UU/NU de la wiki + override explícito de Uber desde el historial del foro; 27 legendarios/míticos fuera de las tablas de elegibilidad quedan con `tier: null` (no disponible en tiers estándar). Cada entrada lleva `source`, `verified: false` y `asOf: "2022-03-24"`.
  - `src/types/pokemon.ts`: tipos `PvpTier`/`TierEntry`. `src/lib/pokedex.ts`: `getTier(pokemon)`, y `searchPokemon()` acepta `filters.tier`.
  - `src/components/TierBadge.tsx`: badge de color propio (uber=rojo, ou=dorado, uu=azul, nu=gris — paleta local, no añadida a `theme/colors.js` por ser de un solo consumidor).
  - `app/pokedex/[id].tsx`: badge de tier junto a los de tipo + aviso amarillo (mismo estilo que `guide.unverifiedBadge`) con la fecha `asOf` interpolada explícitamente, o "no disponible en tiers estándar" para los 27 sin tier.
  - `app/pokedex/index.tsx`: cuarta fila de chips de filtro (Uber/OU/UU/NU), mismo patrón que región/tipo.
  - Verificado en el preview: Charizard→NU, Mew→sin tier (aviso correcto), Wobbuffet→Uber (rojo); filtro "Uber" devuelve exactamente Dugtrio/Wobbuffet/Rayquaza, los 3 esperados.
  - **Pendiente si Ferran quiere subir la fiabilidad más adelante**: verificar en el propio juego si el tier list vigente coincide con este dato de 2022, o repetir la investigación del foro cuando/si alguna vez se publique de nuevo una foto completa en texto plano.

**[BUG ARREGLADO 2026-07-10] "Volver" en pantallas anidadas llevaba a home en vez de al padre lógico.** Reportado por Ferran ("estando dentro de una guía, kanto por ejemplo, si le das atrás te lleva directamente al main menu cuando te debería llevar a lista de guías") — es la MISMA familia de bug que el "te lleva a torneos" reportado antes en esta sesión, solo que esa vez solo se cambió el destino del fallback (`/torneos` → `/`) sin arreglar la causa real. **Causa real, confirmada con un `console.log` temporal**: `router.canGoBack()` de expo-router devuelve `false` con mucha más frecuencia de la esperada — no solo tras un reload web o un deep link (los casos ya conocidos), sino también, se comprobó en el preview, tras navegación normal por `<Link>` en cuanto esa navegación cae a una carga de página completa en vez de un push cliente (se confirmó con `preview_network`: cada click de Link estaba re-descargando `entry.bundle` entero, i.e. reinicia el estado en memoria de react-navigation, que es de donde `canGoBack()` lee). Confiar en `canGoBack()` como única fuente de verdad para "adónde vuelvo" es frágil en general.
- **Arreglo:** `Header` (`src/components/Header.tsx`) ahora acepta una prop `backHref` — el padre lógico de esa pantalla, explícito, puesto por cada pantalla que lo necesita. Sigue intentando `canGoBack()`+`back()` primero (para no perder scroll/estado en el caso normal), pero si eso falla el fallback ya no es un `/` genérico sino el destino correcto: `guia/[region]/index.tsx`→`/guia`, `guia/[region]/tms.tsx`→`` `/guia/${region}` ``, `guia/referencia/[topic].tsx`→`/guia`, `pokedex/[id].tsx`→`/pokedex`, `pokedex/tipos.tsx`→`/pokedex`, `torneos/nuevo.tsx`/`torneos/importar.tsx`/`torneos/[id]/index.tsx`→`/torneos`, `torneos/[id]/export.tsx`→`` `/torneos/${id}` ``. Las pantallas de nivel superior alcanzables directo desde la home (`torneos/index`, `pokedex/index`, `guia/index`, `equipos/index`) no necesitan la prop — el default `'/'` ya es correcto para ellas.
- Verificado en el preview reproduciendo la navegación exacta que describió Ferran (Home→Guía→Kanto→Volver) y un nivel más profundo (Home→Guía→Kanto→TMs/HMs→Volver) — ambos casos aterrizan ahora en el padre correcto, no en home.

**[COMPLETO — datos Y UI, 2026-07-10] Pokédex: filtros por región/tipo + fichas de habilidad/movimiento clicables.** Idea 2 de "Ideas pendientes", terminada de punta a punta:
- **Filtros** en `app/pokedex/index.tsx`: chips de región (`POKEDEX_REGIONS`) y tipo (`ALL_TYPES`), combinables entre sí y con la búsqueda por texto (`searchPokemon(query, {region, type})` en `pokedex.ts`). **Bug real encontrado y arreglado**: un `ScrollView horizontal` sin altura explícita colapsa a ~2px en React Native Web — hace falta `className="h-9 shrink-0 grow-0"` + `contentContainerStyle={{flexDirection:'row', alignItems:'center'}}`, si no las dos filas de chips se solapan visualmente aunque el DOM esté bien.
- **Datos**: `data/reference/moves.json` (556/556) y `data/reference/ability-descriptions.json` (170/170). Fuente principal: PokeMMO Wiki (pokemmo.shoutwiki.com), regla de oro de siempre. Terminado a medias por un agente en 2º plano y a medias a mano por Ferran vía ChatGPT (con verificación cruzada de Claude vía `WebFetch` directo) — combinar ambas fuentes funcionó bien. Validación limpia en ambos archivos (0 dups, 0 campos inválidos, 0 nombres faltantes).
- **UI**: `getMoveData()`/`getAbilityDescription()` en `pokedex.ts` (carga eager, ~250KB combinados — comparable a `ALL_ABILITIES`, no al patrón lazy de `movesets.json`). En `app/pokedex/[id].tsx`, habilidades y movimientos (por nivel, MT, cría, tutor) son pulsables y abren un modal (`MoveDetailModal`/`AbilityDetailModal`, comparten `DetailModalShell`) con tipo/categoría/potencia/precisión/PP/efecto (movimientos) o efecto (habilidades), y un aviso amarillo reutilizando `guide.unverifiedBadge` cuando `verified:false`. Mismo truco de `stopPropagation` en el contenido del modal que ya hizo falta en el visor de imágenes de Guía, para que tocar la tarjeta no la cierre. Verificado en el preview: habilidad (Overgrow), movimiento fijo con aviso (Tackle, `verified:false`), movimiento de estado con potencia nula (Growth, muestra "—").
- **8 entradas (4 movimientos + 4 habilidades) marcadas `verified: false`, por instrucción explícita de Ferran, usando datos de Pokémon oficial Gen 5 en vez de la wiki de PokeMMO** — la wiki de PokeMMO no tenía forma de darlos (páginas rotas/inaccesibles, no porque el dato difiera). Es la ÚNICA excepción a la regla de oro en todo este dataset, y está señalada campo por campo para que cualquier UI futura pueda distinguirlas (mismo patrón que ya usa la app para Farfetch'd/Giratina):
  - **Movimientos:** `Magic Coat`, `Metronome`, `Mirror Move` (las 3 dan error de plantilla Lua en la wiki — `Lua error in Module:Move at line 337: attempt to index field 'flags' (a nil value)`, confirmado repetidas veces; Mirror Move sí tenía Type/efecto confirmados en la propia wiki, solo el PP viene de fuente oficial) y `Tackle` (10 intentos fallidos de carga seguidos, sospecha de timeout por ser la página con el learnset más largo de toda la wiki — se usó la versión Gen 5 Black/White de sus stats, ya que Tackle cambió de potencia entre generaciones oficiales).
  - **Habilidades:** `Air Lock`, `Bad Dreams`, `Toxic Boost`, `Turboblaze` — sin página localizable tras múltiples variantes de URL, confirmado tanto por ChatGPT como por Claude.
  - Si la wiki de PokeMMO arregla estas páginas en el futuro, o Ferran las verifica in-game, reemplazar estas 8 entradas y poner `verified: true`.
- **10 movimientos con página real pero SIN texto de efecto en la wiki** (dice literalmente "No description available", guardado honestamente sin inventar mecánica de los juegos oficiales — estos SÍ quedaron `verified: true` porque type/power/accuracy/pp sí están confirmados, solo falta el texto de efecto): `DoubleSlap`, `DragonBreath`, `ExtremeSpeed`, `Faint Attack`, `Hi Jump Kick`, `Hidden Power`, `Horn Attack`, `Hydro Pump`, `Sand-Attack`, `Selfdestruct`, `SmellingSalt`, `SmokeScreen`, `Softboiled`, `SolarBeam`, `SonicBoom`, `ThunderPunch`, `ThunderShock`, `ViceGrip`.
- **3 habilidades con dato parcial** (SÍ `verified: true`, pero el `effect` deja explícito qué falta), confirmado en una página del wiki DISTINTA a la dedicada a la propia habilidad: `Heatproof`/`Magic Guard` desde `wiki/Status_Effect`, `Magic Bounce` desde `wiki/Magic_Coat` (la página del movimiento).
- Movimientos con potencia variable (no un número fijo): `power: null` + mecánica explicada en `effect` — p. ej. `Wring Out` (120 × ratio de HP del objetivo).

**[COMPLETO 2026-07-10] Nombres de movimiento/habilidad en español.** Ferran detectó que los nombres (no los textos de efecto) salían siempre en inglés aunque la app estuviera en español. **No hizo falta rehacer nada del scraping** — se distinguió entre dato MECÁNICO (potencia/precisión/efecto, sujeto a la regla de oro, viene de la PokeMMO Wiki) y NOMBRE del movimiento/habilidad, que es una traducción oficial fija de Nintendo, igual para cualquier generación/juego — por eso es seguro tomarlo de PokéAPI, igual que ya se hace con sprite/familia evolutiva (única otra excepción a la regla de oro).
- **Fuente:** los CSV en bruto de PokéAPI (`move_names.csv`, `ability_names.csv`, `languages.csv` en `github.com/PokeAPI/pokeapi`), descargados con `curl` directo en vez de `WebFetch` para evitar que un resumen de IA trunque un dataset exhaustivo. `language_id=7` = español de España (`es`, el que usa este proyecto) — **cuidado, no confundir con `id=14`** que es `es-419` (español latinoamericano).
- **`data/reference/move-names-es.json`** (556/556, cobertura total) y **`data/reference/ability-names-es.json`** (168/170 — quedan sin traducir a propósito `Reactive Gas` y `Snow Plow`, exclusivas de PokeMMO sin equivalente en ningún juego oficial, confirmado por búsqueda de substring contra el listado completo de PokéAPI). Mapas planos `{ "NombreInglés": "NombreEspañol" }`, cruce por nombre normalizado (sin espacios/símbolos, minúsculas) + tabla de 4 excepciones manuales para renombres reales entre generaciones (`Faint Attack→Feint Attack`, `Hi Jump Kick→High Jump Kick`, `SmellingSalt→Smelling Salts`, `ViceGrip→Vise Grip` — grafías antiguas de la wiki de PokeMMO vs. nombre oficial moderno de PokéAPI).
- **Arquitectura: la clave de datos NUNCA cambia.** `movesets.json`/`abilities.json`/`moves.json`/`ability-descriptions.json` siguen indexados por el nombre en inglés en todo el código — el nombre en español es puramente una capa de visualización (`localizedMoveName(name, locale)` / `localizedAbilityName(name, locale)` en `src/lib/pokedex.ts`) aplicada solo donde se pinta texto, nunca para lookups/joins. Así no hay ningún riesgo de romper la integridad referencial del dataset ya scrapeado. Con `locale === 'en'` devuelve el nombre tal cual; si falta traducción (las 2 habilidades exclusivas de PokeMMO) cae al nombre en inglés en vez de fallar.
- Conectado en `app/pokedex/[id].tsx`: chips de movimiento (por nivel/MT/cría/tutor), chips de habilidad (normal y oculta) y el título de `MoveDetailModal`/`AbilityDetailModal` — todo vía `useLocaleStore((s) => s.locale)`. Los textos de efecto (`moves.json`/`ability-descriptions.json`) siguen en inglés, no estaban en el alcance de este pedido ("los nombres" específicamente). Verificado en el preview en ambos idiomas (ES: "Placaje"/"Espesura"/"Clorofila" etc.; EN: "Tackle"/"Overgrow"/"Chlorophyll" revierte bien).

**Apartado de Guía completo — las 5 regiones hechas (Kanto, Johto, Hoenn, Sinnoh, Teselia).** Ver detalle completo de cada una en "Apartado de Guía" más abajo. Pendiente suelto que quedó fuera de esta tanda de trabajo:
1. **[COMPLETO 2026-07-10]** `finding-every-move.json` → tutores de movimiento de Hoenn (10), Sinnoh (1, Draco Meteor) y Unova (30, repartidos en Mistralton/Castelia Street/Nacrene City/Opelucid City) + **Battle Frontier** (2 tutores por Puntos de Batalla, 20 movimientos) + **Ultimate Moves** (Blast Burn/Hydro Cannon/Frenzy Plant, con su Pokémon-requisito). Fuente: la misma página ya usada para Kanto (seviichamp.blogspot.com), descargada con `curl` directo (no `WebFetch`) para evitar que un resumen de IA trunque contenido — el HTML tenía `<br style="...">` con atributos que rompían el primer intento de extracción a texto plano (una lista de Unova salió como una sola línea pegada), detectado comparando contra el HTML crudo y corregido con una regex de `<br>` más laxa. Tipo (`FindingEveryMoveGuide` en `src/lib/guides.ts`) y UI (`app/guia/referencia/[topic].tsx`) actualizados con las 3 regiones nuevas + las 2 secciones nuevas; `otherRegionsPending` eliminado. Verificado en el preview.
2. No hay guía de TMs/HMs para Johto ni Teselia. **Búsqueda ampliada 2026-07-11** (antes solo se había comprobado el índice de seviichamp): se revisaron también 2 hilos del foro oficial ("All TM and HM Locations for all regions" — resultó ser solo un post-índice con enlaces a otras guías, sin tabla real; y "TM Shop Locations and Catalog" — bloqueado por el anti-bot de la wiki antes de poder extraer nada) y la propia tabla de la PokeMMO Wiki (`wiki/TM`, 1168 líneas extraídas y analizadas) — esa tabla documenta Kanto/Hoenn/Sinnoh con detalle (ubicación en el mundo + tienda + precio por TM) pero **cero menciones de Johto o de la "Unova Shopping Mall"** pese a que la propia página confirma que esa tienda existe (foto de "Technical Machine Store in Unova Shopping Mall" en la introducción) — simplemente no está catalogada TM por TM ahí. Con 4 fuentes independientes revisadas y ninguna con el dato estructurado que hace falta, se da por cerrada la búsqueda por ahora: si aparece una fuente nueva en el futuro (o Ferran la encuentra jugando), añadirla con el mismo patrón que las otras 3 regiones.
3. Cobertura de objetos parcial en todas las regiones (el parseo es heurístico por regex, no perfecto) y en Johto además no se extrajo el texto narrativo en absoluto (ver nota de Johto más abajo) — si Ferran encuentra huecos jugando, es esperable y se puede ir puliendo bajo demanda en vez de intentar una pasada exhaustiva de una vez.
4. **[COMPLETO — las 5 regiones]** `data/<region>/locations.json` relleno desde la PokeMMO Wiki para Kanto (133 Pokémon, 1282 filas), Johto (75 Pokémon, 383 filas), Hoenn (99 Pokémon, 466 filas), Sinnoh (69 Pokémon, 306 filas) y Teselia (125 Pokémon, 680 filas). Esquema por fila: `{pokemon (slug, mismo que abilities.json), region, locationType, location, levels, rate, timeOfDay[], source, verified}`. `rate` = Very Common/Common/Uncommon/Rare/Very Rare/Horde/Lure/Not specified/Special (Feebas y otros). `locationType` = Tall Grass/Cave/Surfing/Inside/Rocks/Honey Tree/Old Rod/Good Rod/Super Rod/Headbutt (Johto), y en Teselia además **Dark Grass**, **Shadow** (encuentros de silueta voladora, p.ej. Ducklett/Swanna/Rufflet/Vullaby/Sigilyph/Unfezant), **Dust cloud** (Drilbur/Excadrill/Gigalith/Swoobat/Fraxure — mecánica de nube de tierra en cuevas) y **Fishing** genérico (Basculin/Stunfisk usan este valor literal en vez de Good/Good Rod/Super Rod, confirmado con re-fetch específico, no es un error de scraping). Solo se guardan filas de la región propia; los Pokémon evolución-only, legendarios errantes/boss/evento y los "no disponibles en PokeMMO" salen como NONE y simplemente no aparecen (por eso el nº de Pokémon < total regional). **Cuidado con la contaminación de otras regiones**: el scraper devolvía a veces filas de otra región mezcladas (p.ej. Buizel con Route 11/14 de Unova, Misdreavus con Route 209 de Sinnoh, Girafarig con Valor Lakefront de Sinnoh) — se filtran comprobando que la ruta/lugar pertenezca a la región.
   - **Sinnoh terminada (2026-07-09):** los 8 legendarios/míticos pendientes verificados uno a uno — Giratina/Cresselia/Phione/Manaphy/Darkrai/Arceus resultan NONE (boss/raid/evento/fuera de alcance), Shaymin también NONE pese a tener una fila en la wiki (Flower Paradise, nivel 70) porque su `rate` es literalmente "Lunar New Year", un evento anual que no encaja en el enum de `rate` establecido — mismo criterio que Darkrai. **Rotom sí se añadió**: Old Chateau, Inside, nivel 20, Lure — confirmado con dos fetches independientes coincidentes (la sesión anterior no había podido fijar el nivel con confianza).
   - **Teselia terminada (2026-07-09), 156/156 Pokémon procesados:** 125 con encuentro salvaje real (680 filas), 31 confirmados NONE (evolución-only, fósil, boss/raid — Cobalion/Terrakion/Virizion en concreto son raid en Guidance Chamber/Victory Road/Pinwheel Forest — o legendarios "fuera de alcance de PokeMMO" como Tornadus/Thundurus/Landorus/Kyurem/Genesect/Reshiram/Zekrom/Keldeo/Meloetta). Los 3 que se habían quedado fuera de los lotes por un descuido al trocear el roster (scolipede, deerling, sawsbuck) se añadieron en un pase final tras resetear el límite de sesión de WebFetch — deerling introduce el `locationType` "Inside" también en Teselia (Dragonspiral Tower).
   - **Nota de fiabilidad encontrada en Teselia:** Basculin tiene una tabla Wild Locations enorme (~74 filas, surf/pesca en casi toda la región) — dos fetches de recuento total dieron cifras ligeramente distintas (74 vs 71), señal de que estas tablas muy grandes son más propensas a variación entre fetches; se usó la respuesta más detallada (fila por fila) en vez de la de solo conteo. Throh/Sawk y Rufflet/Vullaby (y sus evoluciones Braviary/Mandibuzz) tienen tablas de ubicación **idénticas** entre sí — verificado con re-fetches independientes confirmando el título de página correcto en cada caso — esto es legítimo (son pares de exclusivo de versión que ocupan el mismo slot de aparición), no contaminación cruzada.
   - **[COMPLETO]** Enlazado a la Pokédex: sección "Ubicación" en `app/pokedex/[id].tsx`, agrupada por `locationType`, vía `getLocations()` en `pokedex.ts`.

### Qué está hecho y funcionando

**Fase 1 (gestor de torneos) — completa, con extras más allá del scope original:**
- Bracket de eliminación simple, con byes manejados correctamente (probado a fondo — hubo un bug real de "doble bye" al principio, quedó arreglado y verificado con simulación).
- **Doble eliminación** (`src/lib/doubleElimBracket.ts`, separado de `bracket.ts`): bracket de ganadores + perdedores + gran final única (sin "bracket reset"). **Restringida a participantes potencia de 2 (4/8/16/32)** — un bye no genera perdedor, así que se validó eso en vez de generalizar la lógica. Verificada con 40+ simulaciones antes de tocar la UI.
- Export/import por código de texto (base64, sin backend), con validación de integridad referencial completa en `parseImportedTournament` (rechaza datos corruptos sin crashear).
- Editar nombre, deshacer último resultado (`history` cronológico, reversible sin corromper rondas posteriores), posiciones de sorteo, compartir el bracket como imagen (`react-native-view-shot` + `expo-sharing`).
- **Liguillas (formato `'league'`, añadido 2026-07-03/04):** `src/lib/leagueFormat.ts` — emparejamiento round-robin por el método del círculo (todos contra todos a una vuelta), con un jugador "fantasma" cuando el número de participantes es impar para que cada uno descanse exactamente una jornada. A diferencia de los brackets, una liga **no propaga nada entre partidos** — cada emparejamiento es fijo desde la generación, así que `setLeagueWinner`/`undoLeagueWinner` solo tocan el partido indicado, sin lógica de "siguiente ronda". Clasificación (`computeStandings`) calculada al vuelo a partir de `matches` (3 puntos por victoria, empates a puntos→victorias→derrotas→orden de alta), igual que `getChampion`/`isFinished` se calculan al vuelo para los otros formatos — nunca se guarda un estado derivado. Verificado con 69 simulaciones (n=2..24, 3 tiradas cada uno) antes de tocar la UI: emparejamientos únicos, nadie juega dos veces en la misma jornada, exactamente un descanso por jugador en ligas impares, y un round-trip exacto de deshacer contra cada partido jugado. **Fechas de jornada** (`Tournament.matchdayDates`, opcional, clave = número de ronda) son texto libre introducido por el organizador — puramente informativas, la app no tiene notificaciones ni recordatorios (regla del proyecto). **Filtro de la lista de torneos** por formato y por estado (`app/torneos/index.tsx`), con estado local de UI, no persistido.

**Fase 2 (en progreso) — Pokédex multi-región:**
- **Las 5 regiones hechas — Kanto (151), Johto (100), Hoenn (135), Sinnoh (107) y Teselia (156) — 649 Pokémon en total.** Dataset universal (nombre, tipo, stats base, familia evolutiva, sprite) completo y verificado exhaustivamente.
- `data/<region>/pokemon.json` por región: stats base **verificadas contra la tabla histórica "Generation II-V" de Bulbapedia** (o cruzadas contra las listas de subidas de stats de Gen 6/7/8 si esa tabla no es accesible — le pasó a Sinnoh y Teselia, la tabla se trunca vía WebFetch a partir del dex #386, ver nota de memoria del proyecto), NUNCA contra el endpoint por defecto de PokéAPI ni contra páginas de "stats de generación X" sin verificar que reflejen el juego de esa época y no el actual. **Lección crítica aprendida el 2026-07-04/05:** PokéAPI (y muchas páginas de stats "por generación") devuelven las stats y el tipo del juego más reciente, no de Gen 1-5 — encontramos y corregimos 33 Pokémon con stats post-Gen6/7 y 17 Pokémon con tipo Hada indebido en total. Ver regla de oro más abajo, ahora corregida para reflejar esto. **Vigilar también:** el campo `evolvesFrom` de PokéAPI a veces usa el slug interno en vez del nombre real cuando el nombre tiene un carácter especial (pasó con Nidoran♀/♂ y con Mime Jr.) — revisar cualquier especie nueva con símbolos raros en el nombre. **Y:** si un resumen de búsqueda da una cifra dudosa, verificar contra la tabla/página primaria (p. ej. `serebii.net/sunmoon/updatedstats.shtml`) antes de confiar en el resumen — pasó con Cryogonal.
- `src/lib/pokedex.ts` combina las 5 regiones en `ALL_POKEMON` — necesario porque muchas cadenas evolutivas cruzan la frontera de región (Crobat/Johto evoluciona de Golbat/Kanto, Pikachu/Kanto de Pichu/Johto, Magnezone/Sinnoh de Magneton/Kanto, etc. — Sinnoh es la región con más casos, 22; Teselia no añade ninguno).
- `data/type-chart.json`: tabla de efectividad de tipos **de Gen 5 específicamente** (17 tipos, SIN Hada, Acero resiste Fantasma/Siniestro — cambiado en Gen 6+). Verificada programáticamente.
- Pantallas: lista+buscador, detalle (stats con barras animadas, familia evolutiva con navegación), comparador de tipos interactivo.
- **`abilities.json` y `movesets.json` hechos para las 5 regiones (649 Pokémon)**, scrapeados directamente de la PokeMMO Wiki (pokemmo.shoutwiki.com) — con secciones "Habilidades" y "Movimientos" (agrupados por nivel/MT/cría/tutor) visibles en la pantalla de detalle. Solo Farfetch'd (habilidades+movimientos) y Giratina (solo movimientos) sin verificar, por bugs que la propia wiki reconoce en su origen de datos. `movesets.json` pesa ~5MB combinado — se carga por región bajo demanda (`getMoveset()` en `pokedex.ts`), no todo de golpe. **`pokemmo_tiers.json` completo (2026-07-10)** — ver detalle de Fase 3 arriba, mejor esfuerzo marcado como no verificado desde 2022.
- **`data/kanto/locations.json` completo (2026-07-06)** — encuentros salvajes de los 151 Pokémon de Kanto, scrapeados de la sección "Wild Locations" de cada ficha de la PokeMMO Wiki. Un objeto por fila (Pokémon × ubicación × método), no uno por Pokémon: `pokemon`, `region`, `locationType` (Tall Grass/Cave/Surfing/Inside/Rocks/Old Rod/Good Rod/Super Rod), `location`, `levels`, `rate` (Very Common/Common/Uncommon/Rare/Very Rare/Horde/**Lure**/"Not specified" cuando la propia wiki no especifica nada), `timeOfDay` (subconjunto de morning/day/night, vacío si no hay restricción), `source` (URL de la ficha + fecha) y `verified`. **Sí distingue señuelo:** aparece como valor `"Lure"` de `rate`, no como columna aparte — así que consultar "¿este Pokémon sale por señuelo?" es tan simple como filtrar filas con `rate === "Lure"`. 1282 filas para 133 Pokémon con encuentro salvaje confirmado; los 18 restantes (Venusaur/Charizard/Blastoise/Raichu/Wigglytuff por evolución, Machamp por evolución con piedra de trueque, Farfetch'd por bug conocido de la wiki — mismo que en abilities/movesets —, Porygon por compra en el Rocket Game Corner, Omanyte/Omastar/Kabuto/Kabutops por fósil, Articuno/Zapdos/Moltres/Mewtwo/Mew por mecánica de legendario/evento) se omiten del array a propósito en vez de inventar una fila — confirmado consultando cada ficha, no por omisión. **Aviso de proceso:** esto se generó lanzando un agente en background que a su vez decidió sub-dividir el trabajo en 8 lotes paralelos; varios de esos lotes chocaron con el límite de sesión de Claude a mitad de tarea. El propio agente contenedor tampoco llegó a comitear nada (usó un worktree aislado que se limpió solo al no tener commits). Se rescataron los lotes que sí habían escrito su JSON parcial en el scratchpad (90 Pokémon) y se completaron los 61 restantes a mano en la conversación principal. **Pendiente:** aún no está enlazado a ninguna pantalla de la Pokédex (ver Fase 2 más abajo) ni se ha replicado para Johto/Hoenn/Sinnoh/Teselia — mismo proceso, repetir región por región cuando haga falta.

**Apartado de Guía — las 5 regiones completas (Kanto, Johto, Hoenn, Sinnoh, Teselia):**
- Sección nueva independiente de la Pokédex, accesible desde la home (`app/guia/`). Hub (`app/guia/index.tsx`) con grid de las 5 regiones (solo Kanto activa, resto "Próximamente") + lista de 8 guías de referencia (no atadas a región).
- `data/guides/kanto.json`: walkthrough de Kanto parseado desde la guía de farmeo de seviichamp.blogspot.com (basada en un post de MightyBoxer en el foro de PokeMMO) — 101 tramos en orden de partida real (una misma ciudad puede repetirse si se visita varias veces en la historia, a propósito, para seguir el recorrido real del juego), cada uno con el texto de los pasos y una lista de objetos detectados (198 objetos únicos, con flag `hidden`). El parseo de objetos es heurístico (regex sobre "pick/get a/an/the X") — bastante fiable pero no perfecto; los nombres en crudo se muestran ya legibles gracias a `src/lib/guideItemNames.ts` (diccionario de ~120 objetos comunes + reconocimiento automático de movimientos de TM/HM cruzando contra `data/reference/move-names.json`, generado a partir de los movesets ya verificados de las 5 regiones — así que "TM-rockslide" se muestra como "TM — Rock Slide" sin tener que mantener un diccionario de movimientos aparte).
- **Decisión importante sobre idioma (2026-07-05):** Ferran pidió traducir el walkthrough al español. Se empezó traduciendo las 830 frases línea por línea, pero se paró a las ~50 secciones al notar que eso equivale a recrear la guía completa traducida (un derivado de toda la obra, no una extracción de hechos) — demasiado parecido a reproducir el original. Se probó una alternativa más ligera (una frase-resumen **original** en español por tramo, escrita desde cero, encima del texto en inglés), pero Ferran también la descartó: **si el contenido está en inglés, no debe aparecer nada en español mezclado** — nada de resúmenes en español intercalados con texto en inglés. Se quitó (sin campo `summaryEs`, sin el JSON de resúmenes). **Conclusión para las 4 regiones que quedan:** el walkthrough se deja enteramente en inglés (texto de pasos + nombres de lugar), tal cual la fuente, con atribución — no traducir ni añadir frases en español dentro de ese contenido. La interfaz (botones, cabeceras, "Fuente:") sigue el idioma normal de la app vía i18n, eso no cambia.
- **Corrección critica sobre nombres de lugar (2026-07-05):** se intentó también traducir los nombres de ciudades/rutas a sus nombres oficiales de los juegos en español (p. ej. "Mt. Moon" → "Monte Luna"), pero Ferran corrigió esto: **PokeMMO no traduce los nombres de lugar** — son los mismos para toda la comunidad sea cual sea el idioma del cliente ("Mt. Moon" es "Mt. Moon" siempre). Se revirtió (`location` se muestra siempre en inglés, sin campo `locationEs`) y se corrigieron los 101 resúmenes en español para citar los lugares con su nombre en inglés dentro de la frase. **Es una instancia más de la regla de oro del proyecto** (ver más abajo): no asumir que una convención de los juegos oficiales de Pokémon aplica a PokeMMO sin verificarlo — aplica a nombres de lugar tanto como a movimientos, habilidades o stats. Verificar esto contra Ferran o la wiki de PokeMMO antes de traducir nombres de lugar en las 4 regiones que quedan.
- **Capturas de Gyazo por objeto (2026-07-05):** la guía original enlaza 126 de los 416 objetos detectados a una captura de Gyazo con la ubicación exacta (sobre todo los ocultos, aunque no solo esos). Se extraen del HTML original (el enlace envuelve el nombre del objeto, a veces anidado en `<span><u>`) y se guardan como campo `image` en el item correspondiente — en la UI, el chip de ese objeto es pulsable y abre la captura con `Linking.openURL` (verificado en el preview).
- `data/guides/kanto-tms-hms.json`: tabla de las 50 TMs + 7 HMs de Kanto (tipo/poder/precisión/PP/ubicación), extraída de una guía distinta del mismo blog (basada en pokemondb.net sobre FireRed/LeafGreen, **no verificada específicamente para PokeMMO** — marcado `verified: false` y visible en la UI, la propia guía original avisa de que algunos objetos pueden haberse movido/sustituido en PokeMMO). Ubicaciones traducidas del portugués (única versión disponible de esta guía concreta) al español.
- `data/guides/` también tiene 8 guías de referencia sin atar a región: `ivs`, `effort-values`, `ev-training` (con zonas de entreno reales por Pokémon/ruta/región — dato aprovechable directo para la futura Fase 3), `damage-calculator`, `building-competitive-teams` (resumen en español del framework Strikers/Walls/Guards, no traducción literal), `roaming-legendaries` (FAQ de contenido PokeMMO-específico, parafraseada en español), `finding-every-move` (TMs en tienda + tutores de shard + tutores de movimiento de Kanto, todo con precios/costes reales — Hoenn/Sinnoh/Unova/Battle Frontier/Ultimate Moves del mismo post quedaron sin parsear, ver nota `otherRegionsPending` en el JSON) y `smeargle-sketch`.
- **Nota de atribución importante:** todo el contenido de estas guías es de la comunidad de PokeMMO (blog seviichamp.blogspot.com, que a su vez recopila posts de varios autores del foro oficial — MightyBoxer, Bestfriends, KaynineXL, Damian, FinnTheMember, Akshit — citados en el campo `source` de cada JSON). Los datos de tipo "hechos" (objeto+ubicación, TM+ubicación, zona de EV) se extraen y estructuran; los textos más narrativos/de opinión (la guía de team building, el FAQ de legendarios errantes) se **resumen o parafrasean en español en vez de copiarse tal cual** — no son traducciones literales.
- **Johto (2026-07-06) — `data/guides/johto.json`, formato de extracción distinto al de Kanto:** la guía de farmeo de Johto de seviichamp.blogspot.com (https://seviichamp.blogspot.com/2022/12/pokemmo-farm-guide-walkthrough-johto.html, escrita por CommanderBlue, con nota del propio autor de que se escribió *antes* del lanzamiento de Johto en PokeMMO — puede haber diferencias) está redactada como **prosa narrativa** (con voz propia del autor, chistes, comentarios personales tipo "Sup guys!"), no como instrucciones mecánicas tipo "ve al norte y coge X" como la de Kanto. Reproducir esos párrafos habría sido mucho más parecido a copiar la obra que en el caso de Kanto, así que **no se guardó ningún texto narrativo** — solo se extrajeron objetos por zona (14 tramos, cruzando el texto contra una lista curada de ~65 objetos conocidos de Gen 2 más una regex de números de TM/HM). El campo `steps` de cada tramo se deja vacío a propósito (la UI ya soporta tramos sin texto narrativo, solo con chips de objeto). No se distingue qué objetos están ocultos (esta guía no lo marca de forma consistente) — todo `hidden:false`. Cobertura parcial reconocida en el propio JSON: la prosa puede mencionar objetos que el extractor no detectó. No existe guía de TMs/HMs separada para Johto en el índice (a diferencia de Kanto y Hoenn). **Antes de parsear cada región nueva, comprobar si está en formato mecánico (como Kanto) o narrativo (como Johto) y aplicar el enfoque correspondiente.**
- **Hoenn (2026-07-06) — `data/guides/hoenn.json` + `data/guides/hoenn-tms-hms.json`, mismo formato mecánico que Kanto:** la guía de farmeo de Hoenn de seviichamp.blogspot.com (https://seviichamp.blogspot.com/2022/12/pokemmo-farm-guide-walkthrough-hoenn.html) es del mismo autor (MightyBoxer) y mismo estilo telegráfico que Kanto — se reutilizó el pipeline de extracción tal cual (108 tramos, 366 objetos, 106 con enlace de imagen de Gyazo). También existe una guía de TMs/HMs de Hoenn en el índice (basada en pokemondb.net sobre Emerald, en portugués igual que la de Kanto) — 50 TMs + 8 HMs (Hoenn tiene una HM más que Kanto: Dive), `verified: false` por el mismo motivo que Kanto (refleja el juego original, no PokeMMO específicamente). Ubicaciones traducidas del portugués al español con el mismo criterio mecánico que Kanto (nombres de ciudad en inglés dentro de la frase traducida, ver regla de nombres de lugar arriba).
- **Sinnoh (2026-07-06) — `data/guides/sinnoh.json` + `data/guides/sinnoh-tms-hms.json`, mismo formato mecánico que Kanto/Hoenn:** la guía de farmeo de Sinnoh de seviichamp.blogspot.com (https://seviichamp.blogspot.com/2022/12/pokemmo-farm-guide-walkthrough-sinnoh.html) es del mismo autor (MightyBoxer), mismo pipeline reutilizado — 150 tramos, 655 objetos, 246 con enlace de imagen de Gyazo (la región con más contenido de las 3 hechas hasta ahora). La guía de TMs/HMs de Sinnoh **no está en la sección "Farm Guide" del índice** sino como "Detonado Sinnoh - TMs, HMs, Gyms, Elite 4..." en la sección de walkthroughs — mismo formato de tabla que Kanto/Hoenn (basada en pokemondb.net sobre Platinum) — 50 TMs + 8 HMs (Sinnoh añade Defog y Rock Climb, no tiene Dive), `verified: false` por el mismo motivo. **Lección: al buscar la guía de TMs/HMs de una región nueva, no asumir que solo está en la sección "Farm Guide" del índice — puede estar etiquetada como "Detonado" (walkthrough detallado) en otra sección.**
- `src/lib/guides.ts`: loaders por región/tema con el mismo patrón de `require()` estático que `pokedex.ts` usa para `movesets.json` (Metro no soporta rutas dinámicas). `GUIDE_REGIONS` marca qué región está `available` — cambiar eso y añadir el `case` correspondiente es todo lo que hace falta para activar una región nueva, la UI ya es genérica.
- **Teselia (2026-07-06) — `data/guides/teselia.json`, mismo formato mecánico:** guía de farmeo de Unova de seviichamp.blogspot.com (https://seviichamp.blogspot.com/2022/12/pokemmo-farm-guide-walkthrough-unova.html), mismo autor y pipeline — 88 tramos, 441 objetos, 100 con imagen de Gyazo. Sin guía de TMs/HMs separada en el índice (a diferencia de Kanto/Hoenn/Sinnoh) — solo existen las dos entradas de "Farm Guide" ya conocidas, ninguna con tabla de TMs.
- Pantallas: `app/guia/[region]/index.tsx` (buscador por ruta u objeto + tarjetas colapsables por tramo, con badge de nº de objetos y chips de objeto — los ocultos en un color distinto con icono de ojo tachado; al buscar, los tramos que coinciden se expanden automáticamente), `app/guia/[region]/tms.tsx` (tabla de TMs/HMs), `app/guia/referencia/[topic].tsx` (un render distinto según la forma de cada JSON de referencia — no hay un esquema único porque el contenido origen es muy distinto entre temas).
- **Pasada de calidad (2026-07-06):** auditoría completa tras terminar las 5 regiones. Se encontraron y arreglaron: (1) la introducción de `ivs.json` se había quedado en inglés mientras el resto del documento estaba en español — corregido; (2) 6 restos de portugués sin traducir en las tablas de TMs/HMs (Kanto 1, Hoenn 1 sin tocar en absoluto, Sinnoh 4) — corregidos; (3) `src/lib/guideItemNames.ts` ampliado con ~110 objetos reales que salían como texto pegado sin espacios (`brightpowder` → "Bright Powder", `lifeorb` → "Life Orb", las 16 plates de Arceus, orbes legendarios, fósiles, etc.) más un patrón nuevo para `hm-cut` (Teselia no incluye el número de HM en el nombre, a diferencia de Kanto/Hoenn/Sinnoh); (4) ~30 falsos positivos de la extracción por regex (palabras sueltas de la narrativa que no son objetos reales, como "battle", "grass", "doctor", "one") eliminados de los `items` de las 5 regiones. Buscador (`guide.searchPlaceholder`/`guide.searchEmpty`) añadido a la pantalla de walkthrough — filtra por nombre de ruta o de objeto (ya con el nombre bonito aplicado) y expande automáticamente los tramos que coinciden, útil sobre todo en Sinnoh (150 tramos).
- **Visor de imagen dentro de la app (2026-07-06):** Ferran preguntó si las capturas de Gyazo se verían dentro de la app — hasta entonces el chip solo abría el enlace en el navegador (`Linking.openURL`). Se comprobó que cada página `gyazo.com/<hash>` embebe una imagen directa en `i.gyazo.com/<hash>.png` (confirmado con varias muestras de las 4 regiones que tienen imágenes — Johto no tiene ninguna), así que se añadió `gyazoDirectImageUrl()` en `src/lib/guides.ts` para derivarla, y un modal (`ImageViewerModal` en `app/guia/[region]/index.tsx`) que muestra la captura a pantalla completa con `<Image resizeMode="contain">`, con fallback a "Abrir en el navegador" si la carga falla. Verificado en el preview (imagen cargada con dimensiones reales, no rota). El campo `image` guardado en los datos sigue siendo la URL de la página de Gyazo (la atribuible a la fuente), la URL directa se deriva solo para mostrarla, no se guarda.

**Fases 3-5:** sin empezar (ahora sí desbloqueadas por el dataset completo de 5 regiones; `ev-training.json` del apartado de Guía ya es un input real para la Fase 3).

**Pulido visual y animaciones (2026-07-03/04):** `react-native-reanimated` + `expo-haptics` + `expo-linear-gradient` + `@expo/vector-icons` para feedback táctil, entradas escalonadas, degradados y iconos. **Gotcha importante para cualquier animación futura:** en el preview web de este proyecto (`expo start --web`), las actualizaciones de Reanimated tras el montaje inicial no llegan al DOM (ver `src/lib/animation.ts` y su `isNative`/`nativeOnly()`) — nativo no está afectado, pero cualquier `useSharedValue` cuyo valor final importe para que la UI se vea bien necesita el patrón de fallback documentado ahí. Además, NativeWind no reconoce `className` en componentes `Animated.*` sin registrarlos explícitamente vía `cssInterop` (ver `src/lib/animatedNativewind.ts`).

### Decisiones de diseño importantes (no deshacer sin motivo)

- **Tema oscuro completo** ("UI oscura tipo consola de torneo"), construido vía una sesión de Claude Design + implementación manual — paleta en `src/theme/colors.js` (`ink`/`pokeRed`/`type`/`status`), compartida entre `tailwind.config.js` y componentes RN que necesitan hex crudo (`placeholderTextColor`).
- **Sobre usar Claude Design:** no hace falta para trabajo incremental sobre el design system ya existente — es más lento (no se puede leer el DOM del lienzo, hay que reconstruir valores a mano vía zoom sobre capturas) y tiene límites de cuota de sesión propios. Aporta valor real como "segunda opinión" visual o pasada creativa con ojos frescos, no como paso obligatorio. Usarlo solo si Ferran lo pide explícitamente o para pantallas genuinamente nuevas sin ningún precedente visual.
- **Componentes compartidos:** `Button`, `Badge`(`StatusBadge`), `Card`, `DeleteText`, `VsDivider`, `TypeBadge`, `PressScale` en `src/components/`. Reutilizarlos en vez de duplicar estilos inline.

### Pendiente / sin verificar

- **Nunca se ha probado en un dispositivo/simulador nativo real** — todo el testing ha sido vía `expo start --web`. Cosas concretas sin verificar en iOS/Android: los modificadores de opacidad de Tailwind contra colores custom (`bg-pokeRed/10` etc.), el patrón `group-active` de nativewind, y las animaciones de Reanimated (que en teoría funcionan bien en nativo pero nunca se han visto correr ahí).
- **Icono de la app:** sigue siendo el placeholder genérico de Expo, no hay identidad visual real. Necesita trabajo de diseño real, no algo para improvisar.
- El dataset completo de 5 regiones ya está listo para arrancar la Fase 3 — ver "Estructura de datos" más abajo.

### 💡 Ideas pendientes de desarrollar

**1. [COMPLETO — ver "Apartado de Guía" arriba] Guía por región a partir de seviichamp.blogspot.com.** Las 5 regiones (Kanto, Johto, Hoenn, Sinnoh, Teselia) tienen ya walkthrough + objetos; Kanto/Hoenn/Sinnoh además tienen tabla de TMs/HMs; Kanto además tiene 8 guías de referencia sin atar a región. Sueltos pendientes: ver "🔜 PRÓXIMA SESIÓN" al principio de este archivo (finding-every-move.json de otras regiones, cobertura parcial de objetos, sin TMs/HMs para Johto/Teselia).

**2. [PEDIDO POR FERRAN 2026-07-06 — filtros COMPLETOS 2026-07-10, fichas clicables en curso] Pokédex: filtros + fichas de habilidad/movimiento clicables.** (a) **[HECHO]** Filtros en la lista de la Pokédex por **generación** (`POKEDEX_REGIONS` en `pokedex.ts`) y por **tipo** (`ALL_TYPES`), combinables, chips en `app/pokedex/index.tsx` — usan `ScrollView horizontal` con `className="h-9 shrink-0 grow-0"` + `contentContainerStyle={{flexDirection:'row', alignItems:'center'}}` porque sin altura explícita el `ScrollView` horizontal colapsa a ~2px en RN Web (bug real encontrado y arreglado esta sesión). (b) **[EN CURSO — agente en 2º plano lanzado 2026-07-10]** hacer clicables **habilidades** y **movimientos** en la ficha de detalle, con popup de datos (potencia/categoría/precisión/PP/efecto para movimientos; efecto para habilidades). Necesitaba dataset nuevo — un agente está scrapeando `data/reference/moves.json` (556 movimientos únicos usados en algún moveset) y `data/reference/ability-descriptions.json` (171 habilidades únicas usadas), esquema en `src/types/pokemon.ts` (`MoveEntry`/`AbilityDescriptionEntry`). Es una tanda más grande que la de `locations.json` (~727 páginas) — probablemente necesite varias ejecuciones. **La UI del popup (paso b) queda pendiente de conectar una vez lleguen los datos** — no delegado al agente, mismo patrón que se usó con `locations.json`→Pokédex.

**3. [PEDIDO POR FERRAN 2026-07-06 — COMPLETO 2026-07-10] Selector de idioma ES/EN visible.** `src/store/localeStore.ts` (zustand + persist/AsyncStorage, mismo patrón que `tournamentStore.ts`) + botón "ES/EN" en la home (`app/index.tsx`). Truco clave: `t()` de i18n-js no es reactivo por sí solo (lee `i18n.locale` de forma síncrona), así que `app/_layout.tsx` mete `key={locale}` en el `<Stack>` para forzar un remount completo del árbol cuando cambia el idioma — reevalúa todos los `t(...)` de golpe sin tener que tocar cada pantalla. Verificado: cambia al instante, persiste tras recarga completa.

**4. [PEDIDO POR FERRAN 2026-07-06 — COMPLETO 2026-07-10, trivial] Renombrar tarjeta de la home** a "Gestor de torneos/ligas" / "Tournament/league manager" en `es.json`/`en.json`.

**5. [COMPLETO 2026-07-10] Botón "?" — asistente de preguntas 100% local, sin IA/tokens.** Botón flotante rojo (esquina inferior derecha, `src/components/OracleButton.tsx`, montado en `app/_layout.tsx` así que es visible en toda la app) que abre un modal con buscador de preguntas en lenguaje natural-ish y responde solo con datos ya scrapeados en la app — nunca inventa, si no hay match dice claramente qué sabe responder (`t('oracle.noMatch')`).
   - **No es NLP/IA real** — es un dispatcher de intenciones por patrones/palabras clave + extracción de entidades contra listas ya conocidas. `src/lib/oracle/entities.ts` (`normalize()` + `findPokemon`/`findMove`/`findAbility`/`findType`/`findTypes`, cada uno un índice `{nombre normalizado ES+EN → valor}` ordenado de más largo a más corto, con match por `includes()` — primer acierto gana), `src/lib/oracle/items.ts` (índice perezoso de objetos de las guías de región vía `prettifyItemName()`, prioriza ocultos primero y devuelve la captura de Gyazo si existe, reutilizando `gyazoDirectImageUrl()`), `src/lib/oracle/intents.ts` (`answerQuery(query, locale)`, cadena ordenada de 7 intenciones por regex+entidad — MT/MO/tutor de un movimiento, qué Pokémon tienen tal habilidad, efecto de movimiento/habilidad, debilidades/resistencias — tanto de un Pokémon nombrado como de tipo(s) sueltos vía `getEffectivenessForCombo()` del punto del comparador de tipos —, tipo/stats/evolución de un Pokémon, resumen general si solo se reconoce el Pokémon, y ubicación de objeto como último recurso).
   - **Verificado en el preview** con los 4 ejemplos que ofrece la propia UI: "¿Dónde consigo la MT Tóxico?" (combina tienda + MT6 Kanto/Hoenn/Sinnoh en una sola respuesta), "¿Dónde encuentro a Pikachu?" (4 filas de `locations.json`), "¿Qué hace la habilidad Levitación?" (texto de `ability-descriptions.json`), "Debilidades de Charizard" (x4 Roca / x2 Agua-Eléctrico / x0.5 Fuego-Lucha-Acero / x0.25 Planta-Bicho / inmune Tierra — contrastado a mano contra el tipado real Fuego/Volador, coincide exactamente). El fallback (pregunta sin sentido, p. ej. "cuál es el mejor equipo") devuelve el mensaje honesto de "no sé responder esto, pero sí sé responder X/Y/Z" en vez de inventar.
   - **Limitación conocida, aceptada para v1:** el matching por substring puede dar falsos positivos con nombres muy cortos (Pokémon/movimientos/habilidades de 3-4 letras dentro de una palabra más larga de la pregunta) — no es un problema práctico porque las preguntas son cortas y deliberadas, pero queda documentado por si Ferran lo nota jugando.

**6. [V1 COMPLETA 2026-07-10] Creador/sugeridor de equipos, 100% local, sin IA.** Dado un equipo (hasta 6 Pokémon) + una necesidad declarada por el usuario ("cobertura contra Volador", "equipo balanceado"), sugiere qué Pokémon rellenarían huecos — matemática de cobertura de tipos, no un modelo. Fases:
   - **V1 [COMPLETA]:** `app/equipos/index.tsx` (tarjeta nueva en la home, icono psíquico). Buscador para añadir hasta 6 Pokémon (reutiliza `searchPokemon()` de `pokedex.ts`), sección "Debilidades compartidas" — qué tipos de ataque golpean x2+ a 2 o más miembros a la vez, vía `analyzeTeamWeaknesses()` en `src/lib/teamBuilder.ts` (usa el nuevo `getMultiplier(attackerType, defenderTypes[])` de `typeChart.ts`, que `getEffectivenessForCombo()` también reutiliza ahora) —, y sección "Candidatos que cubren huecos" — Pokémon de las 5 regiones que resisten/son inmunes (≤x0.5) a esas debilidades compartidas, rankeados por cuántas cubren (`suggestCandidates()`), pulsables para añadirlos directamente al equipo. Verificado en el preview: Charizard + Pidgeot (ambos Volador) → debilidad compartida correcta a Eléctrico y Roca → sugiere tipos Tierra (Sandshrew, Diglett, Rhyhorn...), contrastado a mano contra el tipado real (Tierra es inmune a Eléctrico y resiste Roca) — coincide.
   - **V2 [COMPLETA 2026-07-10]:** sección "Cobertura ofensiva" en `app/equipos/index.tsx` — `analyzeOffensiveGaps()` en `teamBuilder.ts` recopila, para cada miembro, el conjunto de tipos de ataque a los que tiene acceso (todo movimiento no-`status` en su `movesets.json` completo — nivel/MT/cría/tutor, no un set de 4 movimientos elegidos, ya que la app no lleva registro de qué movimientos enseñó Ferran en la partida real — deliberadamente optimista, "podría cubrir" no "cubre ahora mismo"), y lista los tipos que NINGÚN miembro puede golpear x2+ con ese repertorio disponible. Verificado en el preview: Magikarp solo → huecos reales en Agua/Eléctrico/Veneno/Volador/Psíquico/Fantasma (coincide con que su movepool es casi solo Agua/Normal); Charizard solo → sin huecos (su movepool de MT/tutor es lo bastante amplio como para cubrir los 17 tipos).
   - **V3 [COMPLETA 2026-07-10]:** filtro "Formato de combate" (Sin restricción/OU/UU/NU) en las sugerencias de candidatos de `app/equipos/index.tsx`, usando `pokemmo_tiers.json` de la Fase 3. `isLegalInFormat()` en `teamBuilder.ts` modela la legalidad ACUMULADA real de PokeMMO (un formato OU permite todo lo no baneado a Uber = ou+uu+nu; UU permite uu+nu; NU solo nu) — deliberadamente NO reordena las sugerencias por "mejor tier", porque la app no sabe para qué formato está construyendo el usuario, así que no le corresponde decidir que un Pokémon OU es "mejor" que uno NU; el filtro solo excluye lo ilegal para el formato elegido. Cada candidato muestra su `TierBadge` y hay un aviso reutilizando el mismo "no verificado desde 2022-03-24" ya usado en la Pokédex. Verificado en el preview: Charizard+Pidgeot (debilidad compartida Eléctrico/Roca) → sin filtro aparecen Dugtrio (Uber) y Nidoqueen/Nidoking (UU) mezclados con NU; filtrando por "NU" esos 3 desaparecen y solo quedan candidatos NU-legales.

**[COMPLETO 2026-07-10] Rol de combate heurístico en la Pokédex.** `src/lib/role.ts` (`computeRole()`) clasifica cualquier Pokémon en `physical`/`special`/`wall`/`speedster`/`balanced` **solo a partir de sus stats base crudas** (comparando bulk=media de PS/Def/Def.esp contra ataque/at.especial y velocidad) — nunca movepool, objetos, ni uso competitivo real. Etiquetado en TODA la UI como heurística explícita, no un dato de PokeMMO verificado (regla de oro): tanto el badge (`RoleBadge`) como el aviso de texto en la ficha (`pokedex.roleUnverifiedNotice`) lo dejan claro. Quinta fila de filtro en `app/pokedex/index.tsx` (Todos/Atacante físico/Atacante especial/Muro/Velocista/Equilibrado) y badge junto a tipo/tier en `app/pokedex/[id].tsx`. Verificado en el preview: Bulbasaur (SpA 65 > Atk 49×1.2) → "Atacante especial" correcto; filtro "Muro" devuelve Onix/Metapod/Kakuna/Jigglypuff/Seel/Dewgong/Drowzee/Hypno/Cubone — todos con sentido intuitivo de bulk alto. Con esto se cierran los 3 huecos que quedaban documentados en el creador de equipos V3.

---

## 🚨 REGLA DE ORO — LEER ANTES DE TOCAR NINGÚN DATO DE POKÉMON

**PokeMMO NO es "Pokémon oficial filtrado hasta Gen 5".** Es un juego con desarrollo propio y continuo. El equipo de PokeMMO decide sus propias mecánicas, y a veces adopta cambios de juegos oficiales posteriores y a veces no, según su propio criterio. Esto significa que **ningún dato de Pokémon (movimiento, habilidad, tier competitivo, mecánica) se da por válido solo porque "es de Gen 5 o anterior".** Hay que verificarlo específicamente contra PokeMMO.

### Jerarquía de fuentes (de más a menos fiable)

1. **Patch notes oficiales de PokeMMO** (foro oficial, sección de anuncios) — máxima autoridad
2. **PokeMMO Wiki** (pokemmo.shoutwiki.com) — documentación mantenida por la comunidad específicamente para PokeMMO
3. **Verificación directa en el juego** (Ferran comprobándolo en persona)
4. **Tier lists / consenso de la comunidad de PokeMMO** (TierMaker "PokeMMO PvP Tierlist", foro oficial — sección Competition Alley)
5. **PokéAPI** — SOLO para sprite y familia evolutiva, que sí son realmente inmutables entre juegos. **NUNCA para movimientos, habilidades, tiers competitivos o mecánicas de combate.**

### ⚠️ Corrección crítica (2026-07-04): ni el tipo ni las stats base son "universales" en PokéAPI

Se asumió originalmente (ver Fase 2 inicial) que tipo y stats base eran datos "inmutables entre generaciones" y por tanto seguros de tomar de PokéAPI tal cual. **Es falso.** PokéAPI devuelve por defecto los datos del juego más reciente, no de Gen 1-5:

- **Tipo:** 22 Pokémon fueron retconados al tipo Hada en la Generación 6 (Hada ni existe en PokeMMO/Gen 5). De los 386 ya construidos, 13 caen en este caso (Clefairy, Clefable, Jigglypuff, Wigglytuff, Mr. Mime, Cleffa, Igglybuff, Togepi, Togetic, Marill, Azumarill, Snubbull, Granbull en Kanto/Johto; Ralts, Kirlia, Gardevoir, Azurill, Mawile en Hoenn).
- **Stats base:** varios Pokémon recibieron subidas de stats permanentes en Gen 6, 7 u 8 (ej. Pikachu, Raichu, Butterfree, Alakazam, Golem... 20 casos solo en Kanto) que PokeMMO (mecánicas Gen 1-5) no tiene.

**Regla corregida:** para tipo y stats base de cualquier Pokémon, contrastar SIEMPRE contra la tabla histórica de Bulbapedia **"List of Pokémon by base stats (Generation II-V)"** (cubre de Gen 2 a Gen 5 sin cambios internos — es una única tabla estable) antes de dar el dato por bueno. No basta con verificar una muestra al azar — hay que comparar la región completa campo por campo, porque comprobar solo unos pocos Pokémon "conocidos de memoria" puede no detectar el problema (esos mismos recuerdos suelen reflejar también el juego actual).

### Reglas duras

- **Smogon NO es fuente para el tier list.** Smogon tiene su propio tier list de Gen 5 (BW) basado en el meta de los juegos oficiales / Pokémon Showdown — esto puede diferir del tier list real dentro de PokeMMO, que tiene su propia liga clasificatoria y su propia comunidad. La fuente del tier list de la app es PokeMMO Wiki + tier lists de comunidad de PokeMMO, nunca Smogon directamente.
- **Todo dato de movimiento/habilidad necesita una capa de overrides manual** sobre PokéAPI, verificada contra las fuentes de arriba. PokéAPI documenta el juego más reciente por defecto — eso está mal para PokeMMO casi siempre.
- **Si un dato no está verificado, no se publica como si lo estuviera.** Mejor un campo vacío o "sin confirmar" en la UI que un dato incorrecto silencioso.
- **Cada entrada del archivo de datos lleva un campo `source`** indicando de dónde salió la información (para poder auditar después).

### Alcance del juego (confirmado)

5 regiones, basadas en estas ROMs específicas:
- Kanto (Pokémon Rojo Fuego)
- Johto (Pokémon SoulSilver)
- Hoenn (Pokémon Esmeralda)
- Sinnoh (Pokémon Diamante/Perla)
- Teselia/Unova (Pokémon Blanco/Negro)

**Ningún Pokémon de Kalos en adelante existe en PokeMMO.** Cero excepciones.

---

## Qué NO hacer en v1

- ❌ Sin anuncios, sin SDK de ads, sin ningún tipo de monetización. Esto puede añadirse en el futuro, pero no es un objetivo de v1 y no debe implementarse por iniciativa propia.
- ❌ Sin backend, sin servidor, sin base de datos remota. Todo vive en el dispositivo.
- ❌ Sin notificaciones push que "vigilen" al usuario (ej. nada de "llevas X horas sin cazar, ¿sigues ahí?"). La app no le habla al usuario salvo que él la mire.
- ❌ Sin usar sprites/assets oficiales de Nintendo si se puede evitar — preferir sprites fan-made/estilizados para minimizar superficie de IP. (Contexto legal completo abajo.)
- ❌ Sin scraping de datos en vivo del GTL — no hay API oficial, es frágil, y PokeMMO Hub ya cubre esa necesidad. No es nuestro terreno.

---

## Contexto legal (resumen, ya investigado a fondo)

- Nintendo/The Pokémon Company tiene historial documentado de tumbar proyectos de fans (Pokemon Essentials, Relic Castle, Pokenet — el propio precedente de un MMO de Pokémon hecho por fans). Riesgo real, no hipotético.
- El propio PokeMMO prohíbe promocionar servicios/negocios no relacionados en su foro oficial y chat del juego — pero SÍ existe una sección "Client Customization" en el foro donde ya conviven herramientas de fans (ej. 3's PokeMMO Tool).
- **Estrategia de distribución recomendada:** perfil bajo. Compartir primero en Client Customization / Discords de la comunidad antes que en tiendas oficiales. Sin monetización visible (ver arriba), que es precisamente la señal que ha atraído takedowns en el pasado.

---

## Stack técnico

- **React Native + Expo (Expo Router)** — mismo patrón que DoDay (`C:\ProyectoClaude\DoDay`): expo ~54, expo-router ~6, nativewind, TypeScript, zustand para estado.
- **i18n bilingüe ES/EN desde el día uno** — `i18n-js` (mismo patrón que MTGAICommander_Verdict).
- **Almacenamiento:** local únicamente. JSON estático empaquetado en la app para los datos del juego (no cambian salvo actualización de la app). Para datos de usuario (progreso, tracker de shinies, torneos) usar AsyncStorage, evaluando expo-sqlite si la complejidad de consultas lo justifica más adelante.
- **Sin dependencias de servidor.** Cero coste de infraestructura, cero mantenimiento de backend.

---

## Features, en orden de construcción (optimizado por dependencia, no por atractivo)

### Fase 1 — ✅ COMPLETA para brackets (ver "Estado actual" arriba); ligas pendientes
**Gestor de torneos/ligas para el grupo de Ferran**
- Bracket simple, participantes, registro de resultados, más doble eliminación, deshacer, renombrar, posiciones y compartir como imagen (no estaban en el scope original, se añadieron después).
- **Decisión de sincronización (confirmada con Ferran, 2026-07-02):** sin backend, sin login ni sesiones. El organizador exporta el estado del bracket como código de texto; los demás lo importan para ver el estado actualizado. Sincronización manual (el organizador re-exporta tras cada ronda).
- **Liguillas (formato round-robin, añadido 2026-07-03/04):** ver detalle técnico en "Estado Actual" arriba. Filtro de la lista de torneos por formato/estado incluido.

### Fase 2 — ✅ COMPLETA (5 de 5 regiones — datos universales, movesets/abilities, locations y tiers, todo hecho y enlazado a la UI)
- **Kanto, Johto, Hoenn, Sinnoh y Teselia hechos (649 Pokémon)**, verificados contra la tabla histórica de Bulbapedia (no PokéAPI por defecto — ver corrección crítica de la regla de oro arriba).
- **Pokédex de consulta** (bilingüe): buscar Pokémon, ver ubicación por región/ruta, stats, movimientos válidos en PokeMMO, tier competitivo (con aviso de no-verificado, ver Fase 3 abajo) — todo enlazado en `app/pokedex/`.
- **Comparador de tipos interactivo — COMPLETO 2026-07-10:** `app/pokedex/tipos.tsx` tiene dos modos con un toggle. **Tipo simple** (ya existía): eliges un tipo atacante y ves qué tipos defensivos individuales son x2/x0.5/x0. **Combinación de tipos** (nuevo): eliges 1 o 2 tipos defensivos (como el tipado real de un Pokémon, p. ej. Planta/Veneno) y ves qué tipos atacantes son x4/x2/x0.5/x0.25/x0 contra esa combinación — `getEffectivenessForCombo()` en `src/lib/typeChart.ts`, multiplica el multiplicador de cada tipo defensivo por separado (mismo cálculo que usa el juego). Verificado contra Planta/Veneno (Bulbasaur): x2 a Fuego/Hielo/Volador/Psíquico, x0.5 a Agua/Eléctrico/Lucha, x0.25 a Planta — coincide con el tipado real conocido.

### Fase 3 — ✅ V1 COMPLETA (crianza y tier list hechos; refinamientos posibles más adelante)
- **Calculadora de crianza — [V1 COMPLETA 2026-07-10, ver detalle arriba]:** `app/cria/index.tsx`, herencia de IVs (sistema propio de PokeMMO, no el de los juegos oficiales) y naturaleza por Everstone, para un solo cruce. **[AÑADIDO 2026-07-10] Tabla de coste aproximado por objetivo de IVs** (1x31 a 6x31, con/sin naturaleza) en `data/guides/breeding.json`/`app/guia/referencia/breeding` — cifras transcritas TAL CUAL de la guía del foro (930k/1.8M para 6x31, etc.), sin inventar ninguna fórmula nueva. **Deliberadamente NO se construyó un planificador automático genérico** (tipo "dame la cadena de cruces óptima para llegar a un 5x31 concreto desde cero con estos progenitores") — la guía original solo documenta a mano dos ejemplos completos (6x31 sin/con naturaleza), no una fórmula recursiva general para cualquier combinación intermedia; inventar esa generalización sin verificarla habría sido arriesgar un cálculo incorrecto en un proyecto real de horas/dinero del juego, así que se dejó fuera en vez de adivinar. Si se retoma en el futuro, la guía ya documenta el procedimiento paso a paso manual (Gilan, foro oficial) como punto de partida para diseñarlo con cuidado, no para copiarlo directamente en código.
- **Tier list competitiva — [COMPLETA 2026-07-10, ver detalle arriba en "🔜 PRÓXIMA SESIÓN"]:** badge de tier + filtro Uber/OU/UU/NU en la Pokédex, con aviso explícito de "no verificado desde 2022-03-24" (no hay fuente pública en texto con foto completa y actual — el sistema real se recalcula en vivo por uso).
- **Rol de combate heurístico — [COMPLETO 2026-07-10, ver detalle arriba]:** filtro y badge en la Pokédex (`src/lib/role.ts`), basado solo en stats base crudas, etiquetado en toda la UI como heurística no verificada — nunca se hace pasar por un dato real de PokeMMO.

### Fase 4 — ✅ COMPLETA
- **Tracker de shinies/encuentros con widget pasivo — [COMPLETA 2026-07-10, ver detalle arriba]:** `app/shinies/index.tsx` + `src/store/shinyStore.ts` + widget en `app/index.tsx`.
- **Exportar equipos a formato Showdown — [COMPLETA 2026-07-10, ver detalle arriba]:** `app/equipos/index.tsx` + `src/lib/showdown.ts`, set completo por miembro (objeto/habilidad/naturaleza/EVs/IVs/movimientos) exportable a texto Showdown con copiar/compartir.

### Fase 5 — ✅ COMPLETA
- **Calculadora de daño — [COMPLETA 2026-07-10, ver detalle arriba]:** `app/danio/index.tsx` + `src/lib/damageCalc.ts` + `src/lib/stats.ts`. Fórmula base de Gen 5 verificada a mano; clima/multigolpe/objetos-habilidades específicos deliberadamente fuera de alcance (documentado en la propia UI).

---

## Estructura de datos propuesta (punto de partida, ajustar según se construya)

```
/data
  /kanto
    pokemon.json       // stats base + tipo (fuente: tabla Gen II-V de Bulbapedia, NUNCA PokéAPI por defecto), familia evolutiva/sprite (fuente: PokéAPI, sí válido tal cual)
    movesets.json       // movimientos válidos en PokeMMO por Pokémon (fuente: overrides manuales, campo "source" obligatorio)
    abilities.json       // habilidades válidas en PokeMMO por Pokémon (fuente: overrides manuales)
    locations.json       // encuentros salvajes, una fila por Pokémon x ubicación x método (fuente: PokeMMO Wiki, sección "Wild Locations" de cada ficha) — hecho para las 5 regiones
  /johto, /hoenn, /sinnoh, /teselia (misma estructura — las 5 regiones completas)
  /tiers
    pokemmo_tiers.json   // fuente: PokeMMO Wiki (elegibilidad OU/UU/NU) + historial del foro (Uber) — completo pero NO verificado desde 2022-03-24, ver Fase 3
```

Cada entrada de `movesets.json` y `abilities.json` debe incluir:
```json
{
  "pokemon": "charizard",
  "value": "...",
  "source": "PokeMMO Wiki — verificado 2026-XX-XX",
  "verified": true
}
```
